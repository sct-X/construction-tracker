Critique after the redesign (impeccable critique, 2026-09-18, fresh reviewer; same method and headings as DESIGN_CRITIQUE_BEFORE.md).
Method: dual-agent (A: design review over 37 screen states at 1280 and 390 · B: detector + browser evidence). Detector: 12 advisory findings, all "radius outside DESIGN.md" (2-3px on Gantt bars, chips, a call-list key), none structural. Health score: **30/40** (visibility 3, real world 3, control 3, consistency 3, error prevention 3, recognition 3, efficiency 3, aesthetic/minimal 3, recovery 3, help 3). Before: 22/40.
Specificity verdict: authored. Graphite tonal ladder, Barlow figures over Atkinson words (both WOFF2 200, computed font-family confirmed), dates as the largest type on every partner screen (32px rows, 60px hero, 22-32px titles), statuses as words on a wash with a leading "!", one hi-vis control per screen (verified: 0-1 primary on all 37 states; the Gantt today line is the only other orange). No gradients except the shutdown hatch, no caps, no horizontal scroll, money absent from every Alec DOM, offline a 47px amber line, focus ring orange, reduced motion honoured, muted text 5.9:1.
P1 Raff's home (`/waiting?owner=me`, phone) is 7,594px: the "Later" group (3,762px) starts open (`WaitingOn.tsx:333`), so the P1 from before is only half fixed.
P1 Step detail (`StepDetail.tsx:168`) paints "Mark done" orange on a hold point that will refuse (1 of 3 photo sets); the primary should be "Add photos" until the sets exist.
P2 Two page skeletons for sibling screens: build overview is back-link, title, tabs, hero, primary; the design checklist (`DesignChecklist.tsx:132`) is title, primary, tabs, hero, no back link.
P2 Two filter idioms: segmented troughs on Monday, Program and Waiting on; loose raised chips on Notifications (`notifications.css:50`); the sidebar calls that screen "Activity" (`nav.ts:72`) and the page calls itself "Notifications".
P2 Alec's screens miss 56px in three places: "Add a photo" text link on Daily notes 23px (`noteEntry.css:96`), "Mark all read" 36px on Notifications, the Look-ahead/Full program and iPhone/Android segments 44px. The call list on a phone makes the phone number an 18px inline link that wraps mid-number (`CallItem.tsx:216`).
P2 Restated copy survives in two spots: "Add photos" plus "Add photos now" in the same viewport on Alec's Today (`HoldPointCheck.tsx:118`); "Done on the phone. This tab is a browser." on Settings (`Settings.tsx:199`). Monday's amber "Last confirmed 9 days ago" chip wraps to three lines in a 110px column (`monday.css:217`).
Persona flags: Dom still adds rows to get total slip; Alec's pickers are 56px buttons and nothing shows him a price; Raff's act-by date now leads every row, but he scrolls past 18 "in 17 weeks" items every morning.
