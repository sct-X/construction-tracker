# Contracts for screen subagents

Stage 0 fixed these. Build on them; do not edit `src/domain/*`, `src/data/api.ts`, `src/seed/*` or `src/styles/tokens.css` unless your task says so. Report the need instead.

## Getting data

```tsx
import { useApi, useQuery, useSession } from '../data/context';

const api = useApi();                                   // TrackerApi (src/data/api.ts)
const { person, role, side, sides, today, offline, setSession } = useSession();
const jobs = useQuery((api) => api.listJobs(), []);     // re-runs after every api change
const forecast = useQuery((api) => api.getForecast(jobId), [jobId]);
```

- Reads are synchronous and return copies. Never mutate a returned object; call a mutation.
- `useQuery(fn, deps)` re-runs when `deps` change or when the api notifies (`api.subscribe`). No loading state is needed for the mock; still render the shared empty states from UI_PLAN.
- Every read is already scoped to the session's side and role. `listJobs()` hides templates and, for Alec, design jobs; `listItems()` for Alec returns material items only (his deliveries). `getMondayRows()` is empty for the site role. `canSee(screenKey)` tells navigation what to show (`SCREEN_ACCESS` in api.ts, keys in `src/screens/README.md`).
- Mutations return the new record, log an activity entry as the current person, and notify. `setStepStatus(stepId, 'done')` on a hold point returns `{ ok: false, missingCategories, message }` when required photo categories are empty; show `message` as is.
- Photo queue: `queuePhoto(input)`, `listQueuedPhotos()`, `removeQueuedPhoto(id)`, `flushPhotoQueue()` are async (IndexedDB). The provider flushes automatically when `offline` turns false; the shell's `<QueueBadge>` flushes on the browser's `online` event and retries failed sends. Only uploaded photos (`addPhoto` / a successful flush) count for hold points. See "Photos and the queue (Stage 3)".
- Offline rule: with `offline` true, date changes (expected date, ETA, durations, links) must be disabled in the UI with "Needs signal". Status ticks, notes and photos queue. The mock does not enforce this; the screen does.

## Session

`Session = { personId, sideId, today, offline }` in `src/data/session.ts`, persisted in localStorage. URL params override it on load and on every hash change: `#/monday?as=alec&today=2026-09-17&offline=1&side=side-nd`. Playwright specs use those. The dev bar (`src/dev/DevBar.tsx`) has `data-testid`s `dev-person`, `dev-side`, `dev-today`, `dev-offline`, `dev-reset`, `dev-fire-reminders`. Reset reseeds the data, clears the photo queue, and returns today to 2026-09-17 and offline to false; the person stays.

People: `dominic` (admin), `dom`, `norm` (partners), `raff` (builder), `alec` (site). Sides: `side-nd` "Norm and Dom", `side-norm` "Norm" (empty). Jobs: `park-rd`, `seaview`, `beatty`, `west-st`, `tollbar`, `lower-beach`, `john-st`; template `tpl-duplex`. Windows shipment `sh-park-windows`.

## Forecast result (`JobForecast`, src/domain/forecast.ts)

- `forecastFinish`, `plannedFinish`, `lateDays` (forecast vs planned), `isLate`.
- `snapshotDate`, `snapshotFinish`, `slipDays`, `slipCost` (vs last Monday; both undefined before the first snapshot, show a dash and "Slip appears after the first Monday"). `slipSincePlanDays`, `slipSincePlanCost` for the Monday toggle.
- `freshness: { lastConfirmed, daysUnconfirmed, amber, text }`. Amber after 7 days. Show `text`.
- `currentStageId/Name`, `stages[]` (bands with planned and forecast start/end, `lateDays`, derived `status`), `steps{}` by id (`forecastStart/End`, `plannedStart/End`, `lateDays`, `driver`, `reason` sentence, `waitsFor`, `holdsUp`, `itemIds`), `items{}` by id (`neededBy`, `actBy`, `expected`, `expectedFromShipmentId`, `lateDays`, `isLate`, `lateText` like "14 days late", `actByPassed`, `daysSitting`).
- `holdPoints[]` and `nextHoldPoint` (`required[]` with `uploadedCount`, `missingCategories`, `ok`).
- `whyItMoved[]` (against last Monday when the snapshot holds step dates, else against plan) and `whyItMovedSincePlan[]`: entries `{ kind: 'cause'|'step'|'stage'|'finish', baseline: 'plan'|'snapshot', text, deltaDays, from, to, refId }`. Render `text` in order. Finish lines name their baseline ("7 days later than planned (27 Nov)", "5 days later than Monday's snapshot (29 Nov)"); when a snapshot has no step dates the chain runs against the plan and ends with both finish lines.
- Design jobs: `checklist` with `stages`, `currentStageName`, `outstanding`, `oldestDays`, `outstandingItems[]`; no finish, no steps.
- `api.previewEtaChange(shipmentId, newEta)` returns `{ linkedItemIds, movedSteps[], finishBefore, finishAfter, deltaDays, slipDaysAfter, slipCostAfter, costDelta }` for the impact panel.
- `api.getMondayRows()` returns `MondayRow[]` sorted builds by slip cost then design by oldest item, with `waitingOn` (top three) per build job.

## Shell and shared components (Stage 1)

- Routes: one registry, `src/shell/routes.tsx` (`path`, `screen` key for `canSee`, `title`, `stage`, `element`). Every screen is built; to add one, add an entry there and App.tsx maps the list to `<Route>`s inside `<Guard>`. A gated route the role cannot see renders the `no-access` refusal, the same for every route. Keep each entry on one line starting with its path: `e2e/no-money-for-site.spec.ts` reads the paths out of that file and visits every one as Alec.
- `useLayout()` from `src/shell/AppShell` returns `'phone' | 'desktop'` (`'phone'` below 768px and always for the site role). Switch cards and tables on it so every test id exists once.
- `<PageHeader title meta actions back />` (`src/shell/PageHeader.tsx`) opens every screen; wrap the screen in `<main className="page">`. Shared classes in `src/shell/shell.css`: `.page`, `.page__lede`, `.btn`, `.btn--primary` (hi-vis, dark ink), `.btn--desktop`.
- `<StatusText tone="late|amber|ok|muted|plain" testId="...">words</StatusText>` (`src/components/StatusText.tsx`) draws status words on a wash; late and amber get a leading "!". Always words, never a bare colour.
- Shared components take a `testId` prop (never `data-testid`): `Money`, `BigNumber`, `SlipText`, `StatusText`.
- `<ItemRow item forecast ownerName href context action when testId />` (`src/components/ItemRow.tsx`) is the one item row. `when` (Stage 4) replaces its single date phrase: `{ text, tone }` draws those words instead of the calculator's default, `null` draws no date line, undefined keeps the default. `action` renders outside the link.
- Freshness (rule 7) comes only from the calculator: `getForecast(jobId).freshness` (`amber`, `daysUnconfirmed`, `text`). Screens may reword it ("Unconfirmed 9 days") but never recompute it.
- Nav test ids: `nav-<name>` (`nav-monday`, `nav-waiting`, `nav-calls`, `nav-jobs`, `nav-today`, `nav-upload`, `nav-shipments`, `nav-activity`, `nav-templates`, `nav-trades`, `nav-people`, `nav-notifications`, `nav-settings`, `nav-job-<id>`); `primary-nav`, `setup-nav`, `side-switcher` / `side-name`, `offline-bar`, `bell-badge` (the unread count on the bell, `<BellBadge>` in `src/components/BellBadge.tsx`, nothing at zero).

## Program and Gantt (Stage 2)

- `<Gantt forecast steps today view? dense? onSelectStep? renderBar? />` (`src/components/gantt/Gantt.tsx`) draws the desktop program from `getForecast(jobId)` plus `listSteps(jobId)`. Read-only by default: every bar is a link to `#/steps/:id`. `view` is `'all' | 'lookahead' | 'late'`.
- Stage 6 (program editor) reuses it rather than forking: `onSelectStep(stepId)` turns each bar into a button that calls back instead of navigating; `renderBar(step, geometry, defaultBar)` replaces what is drawn in a step's track (return your own handles or wrap `defaultBar`). `geometry` is `{ x, width, plannedX?, plannedWidth?, isHoldPoint, pxPerDay, rowHeight }` in px from the track's left edge; the planned outline stays behind whatever you draw and the late text after it. The axis (`timeScale.ts`, `makeTimeScale({ dates, today })`) is calendar days at 5px a day; `scale.x(iso)` and `scale.span(start, end)` convert dates.
- Phone pieces: `<StagesStrip forecast />` (chips, current stage `aria-current`), `<LookAhead forecast steps items today />` (three weeks plus Later), `<StagesList forecast today />` (bands). Test ids: `gantt`, `gantt-bar-<stepId>`, `gantt-planned-<stepId>` (`data-moved`), `gantt-late-<stepId>`, `gantt-row-<stepId>`, `gantt-stage-<stageId>`, `gantt-today`, `gantt-caption`, `gantt-empty`, `program-view-<all|lookahead|late>`, `program-edit`, `program-full-link`, `lookahead`, `lookahead-week-<1..3>`, `lookahead-step-<stepId>`, `stages-strip`, `stage-chip-<stageId>`, `stage-band-<stageId>`.

## Money

Fields in `MONEY_FIELDS` (`src/domain/money.ts`): `weeklyHoldingCost`, `slipCost`, `slipSincePlanCost`, `costDelta`, `slipCostAfter`. The data layer deletes them for the site role. Render money through one `<Money value={x} label="..."/>` component that returns `null` when `value === undefined`, label included. Never hide money with CSS, never default it to 0. Format with `formatMoney()` ("$9,000"). Activity and notification text never contains money.

## Dates

All dates are `YYYY-MM-DD` strings. Helpers in `src/domain/dates.ts`: `formatLong` "Fri 26 Feb 2027", `formatShort` "Mon 2 Nov", `formatDayMonth` "2 Nov", `formatDayMonthYear2` "12 Mar 27", `formatWeekRange(monday)` "14-18 Sep", `relativeDays(from, today)` "9 days ago", `formatDelta(14)` "+14 days", `formatStamp` "Tue 3:10pm", `lastMonday`, `addCalendarWeeks`, `addWorkingDays`, `isWorkingDay`, `calendarDaysBetween`. Use the session's `today`, never `new Date()`, for anything the user sees.

## Test ids

`data-testid` on every primary control and every row, kebab-case, `<screen>-<thing>[-<id>]`: `monday-row-park-rd`, `monday-slip-park-rd`, `item-status-advance`, `upload-category-pc-lockup-windows`, `step-mark-done`, `shipment-eta-input`, `shipment-save-eta`, `shipment-eta-preview`, `shipment-add`. Every id starts with its screen's name, including the shared component's ids inside it (`<EtaImpact>` renders `shipment-eta-preview-finish`, `shipment-eta-preview-slip`). Shipments: `addShipment`, `linkItemToShipment(itemId, shipmentId | null)` and `setShipmentEta` already exist on the API and log activity; the Stage 2 screens use them unchanged. Flow specs in `e2e/` find things by these ids and by visible text; give both.

## Tokens and styling

One token set in `src/styles/tokens.css`, read the plan at the top. Use the variables, never raw hex. Semantic colour always comes with words (`--late-ink` on `--late-bg` plus "14 days late"). Hero figures use `.display` (condensed, tabular). Tap targets `min-height: var(--tap)` (56px) on every phone control; category pickers are buttons. Phone below 768px: `@media (min-width: 768px)` switches cards to tables. One CSS file per screen, class names `screen__part`. Hairlines over shadows; shadow only for sheets and menus. No gradients, no all-caps labels, no icons library, no stock imagery.

## Photos and the queue (Stage 3)

- `src/data/photoQueue.ts`: `sharedPhotoQueue()` is the browser's one queue (the api sends through it; `createPhotoQueue()` returns it when IndexedDB exists, a fresh memory queue otherwise). `PhotoQueue`: `enqueue(entry)`, `list()`, `get(id)`, `update(id, patch)`, `remove(id)`, `clear()`, `subscribe(listener) => unsubscribe` (fires after every change), `flush(upload, canSend)` (one at a time, oldest first, single-flight, re-reads each entry before sending so a Remove mid-flush wins; a failure marks the entry `failed` with `attempts + 1` and the loop stops only when `canSend()` is false). `QueuedPhoto`: `id`, `sideId`, `jobId`, `stageId`, `categoryId`, `dataUrl` (what the mock uploads, downscaled to 800px on the phone), `thumbDataUrl` (160px, for lists), `blob` (the original File, for the real server), `takenOn`, `uploadedById`, `queuedAt`, `state: 'queued' | 'sending' | 'failed'`, `error`, `attempts`. `sortQueued(list)` orders oldest first.
- `src/components/QueueBadge.tsx`: `useQueuedPhotos(jobId?)` is the ONE hook for reading the queue on any screen (gallery, Today, upload, queue, badge); it refreshes on queue and api changes. `noSignal(offline)` = the session flag or `navigator.onLine === false`. `<ClockGlyph />` is the queued mark, always beside words. `<QueueBadge />` (mounted by the shell after `<OfflineBar />`, `queue-badge`, hidden at zero and on `/queue`) also owns the automatic sends: flush on `online`, retry after a failure with 5s x attempts (max 60s) up to `MAX_AUTO_ATTEMPTS` (5); the queue screen's Retry resets `attempts` and re-arms it.
- `<CategoryPicker categories counts queued? value onChange neededWords? testPrefix? label? />` (`src/components/CategoryPicker.tsx`): tall radio buttons, chosen one filled steel, count in the display face, "none yet", "N waiting to send"; `neededWords(category)` supplies the words for a required category. Test ids `<testPrefix>-<categoryId>`, default `upload-category-<id>`.
- `<HoldPointCheck check refusal? queuedCount? uploadHref? compact? canComplete? testId? />`: `canComplete` (default: the session role is not `site`) chooses the queued sentence, "You can tick this off once they've sent." or "They count once they've sent." Test ids `holdpoint-readiness`, `holdpoint-category-<id>`, `holdpoint-refusal`, `holdpoint-queued`, `holdpoint-add-photos`.
- Screens and test ids: upload `#/jobs/:id/upload?stage=&category=` (`photo-upload`, `upload-stage-<id>`, `upload-category-<id>`, `upload-file-input`, `upload-camera-input` (phone only), `upload-choose`, `upload-take`, `upload-preview-<n>`, `upload-remove-<n>`, `upload-submit`, `upload-progress`, `upload-offline-note`, `upload-failed`, `upload-done`, `upload-finish`, `upload-more`, `upload-no-categories`, `upload-problem`); queue `#/queue` (`upload-queue`, `queue-send-now`, `queue-offline-note`, `queue-group-<jobId>-<categoryId>`, `queue-item-<id>` with `data-state`, `queue-retry-<id>`, `queue-remove-<id>`, `queue-empty`).
- Service worker (`public/sw.js`): cache lookups pass `{ ignoreVary: true }` because `vite preview` (and some hosts) answer with `Vary: Origin` and a `<script crossorigin>` request would otherwise miss its precached response. `e2e/offline-shell.spec.ts` loads once, cuts the connection with `context.setOffline(true)`, reloads and expects the app.

## Call list (Stage 4)

- `api.finishCall({ personId, jobs: [{ jobId, itemsUpdated }] })` ends a call: every listed job gets `lastConfirmed = today` (rule 7's clock resets; freshness reads "Last confirmed today") and one `job_confirmed` activity entry per job in the call's words, "Dominic rang Raff: 3 items updated on 64-66 Park Rd" ("nothing changed" at zero). Returns `{ jobs, activity }` and notifies once. Item changes made during the call go through the existing `updateItemStatus`, `setItemExpectedDate` and `updateItem({ notes })`, each logging as usual.
- `<CallItem>` (`src/components/CallItem.tsx`) is one line of the script: act-by date in the condensed face with "act by, 5 weeks ago" words (`agoWords`), the item in words, the trade's number as a `tel:` link, one primary action from `nextStatus(item)` ("Mark ordered" for material, "Mark booked" for trades, inspections and defects, "Mark done" for the rest; "Mark confirmed" asks for the date the trade is coming and sets the expected date with the status), an inline expected date (disabled offline or when it comes from a shipment), a note, Skip. A handled row renders folded (`call-done-<id>`) with "Back on the list".
- Screen `#/calls?person=<id>&scope=all` (`call-list`): the cut is UI_PLAN 3.11 (`inCallCut`: to do or booked, act-by within 14 days or past); `scope=all` lists every open item instead. The script is planned once per call and held still (marking rows or moving dates never reorders it or drops rows mid-call). Test ids: `calls-count`, `calls-people`, `calls-person-<id>`, `calls-scope`, `calls-keys`, `calls-job-<jobId>`, `calls-fresh-<jobId>`, `calls-finish-<jobId>` (the finish date line), `calls-holding-<jobId>`, `calls-nothing-<jobId>`, `calls-empty`, `call-item-<id>`, `call-actby-<id>`, `call-phone-<id>`, `call-action-<id>`, `call-confirming-<id>`, `call-confirm-date-<id>`, `call-confirm-<id>`, `call-more-toggle-<id>` (phone), `call-more-<id>`, `call-expected-<id>`, `call-note-<id>`, `call-skip-<id>`, `call-done-<id>`, `call-reopen-<id>`, `call-moved-<jobId>` ("Beatty St finish moves +7 days, $2,000, now Fri 11 Dec"), `calls-done-<jobId>`, `calls-finish` (the button), `calls-finish-panel`, `calls-confirm-<jobId>` (tick), `calls-finish-confirm`, `calls-finish-cancel`, `calls-summary`, `calls-summary-<jobId>`, `calls-again`.

## Waiting-on list and item sheet (Stage 4)

- `#/waiting` (`src/screens/WaitingOn.tsx`, `waiting-on`) reads its filters from the hash query: `?owner=me|<personId>` (`me` is the current person; Raff's home is `#/waiting?owner=me`, titled "My items"), `?job=<jobId>` (also sets the back link and `waiting-add` to `#/items/new?job=<id>`), `?type=<ItemType>`. The side is the session's. Groups: `waiting-group-overdue|this-week|next-week|later|done` (done is a collapsed `<details>`); rows `item-row-<id>` (the shared row on the phone, a `<tr>` on the desktop; both open `#/items/:id`); controls `item-advance-<id>` (the one status button, words from `advanceLabel`), `item-call-<id>` (tel: link when the trade has a phone), `item-set-date-<id>` → `item-date-input-<id>`, `item-date-save-<id>`, `item-date-cancel-<id>` (hidden offline: "Date needs signal"); filters `waiting-filter-mine|anyone|owner-<personId>|all-jobs|job-<jobId>|all-types|type-<ItemType>` (buttons, `aria-pressed`). Exports: `groupFor(item, forecastItem, today)`, `keyDate(item, forecastItem)` (act-by until confirmed, then expected or needed-by), `rowWhenWords(item, forecastItem, today)`.
- Grouping: an item is Overdue when the calculator says it is late or its key date has passed; This week and Next week run Mon to Sun from `lastMonday(today)`. UI_PLAN 3.8's status, waiting-on and time filters are covered by the groups plus the Done details, not separate controls.
- `src/domain/itemFlow.ts` is the ONE status flow for every list: `nextStatus(item)` → `{ status, label }` or null, `advanceLabel(item)`. Trade, inspection, defect: Mark booked → Mark confirmed → Mark done. Material: Mark ordered → Mark confirmed → Mark done. Consultant report, council request: Mark requested → Mark received → Mark done (the model's `booked` and `confirmed`). Decision, reminder, condition of consent: Mark done. The waiting list and the call list both import it; call `updateItemStatus(id, next.status)`, not `advanceItemStatus`, which cannot skip `booked`.
- `agoWords(iso, today)` lives in `src/domain/dates.ts` ("today", "yesterday", "tomorrow", "3 days ago", "in 8 days", "5 weeks ago", "in 2 weeks").
- `#/items/:id` and `#/items/new?job=&step=&type=&photo=` (`src/screens/ItemSheet.tsx`, `item-sheet`): one form for reading, adding and editing. Ids: `item-title`, `item-type-<type>` (`<ItemTypePicker value onChange label? testPrefix? />` in `src/components/ItemTypePicker.tsx`, radio buttons, `aria-checked`), `item-job-<jobId>`, `item-step` (select), `item-trade` (select) / `item-waiting-on` (free text) / `item-ring` (tel:), `item-owner-<personId>`, `item-lead-time`, `item-needed-by` (a date input, or the read-only calculator figure with "from step X" when a step is linked), `item-act-by` (always derived: needed-by minus lead weeks, recomputed from the draft), `item-expected` (a date input, or read-only "Comes from shipment" with `item-shipment-link`), `item-late` (the calculator's late words), `item-status-<status>`, `item-confirmed`, `item-notes`, `item-photo-link` / `item-photo-pick-<photoId>` / `item-photo-take` / `item-photo-clear` (defects), `item-problems` (validation words), `item-save`, `item-cancel`, `item-saved`, `item-delete` + `item-delete-confirm` / `item-delete-keep` (admin and partners), `item-history`. Saving an edit calls `updateItemStatus` when the status changed, then `updateItem`; a new item goes through `addItem` and lands on `#/waiting?job=<id>`. `addItem` throws when `stepId` is not on `jobId`.

## Design checklist, notifications and the buzz (Stage 5)

- `#/jobs/:id` for a design job renders `src/screens/DesignChecklist.tsx` (`checklist`). Its figure is `getForecast(id).checklist` in the jobs list's words ("2 outstanding, oldest 23 days", `checklist-outstanding`); `outstandingWords(forecast)` and `sittingWords(days)` are exported. Stages: `checklist-stage-<stageId>` (`data-status`), status buttons `checklist-stage-status-<stageId>-<not_started|in_progress|done>` (`aria-pressed`; admin and builder) or words `checklist-stage-words-<stageId>` (partners); open items under the current stage as `<ItemRow testId="checklist-item-<id>">` with `when` = "outstanding N days"; `checklist-current`, `checklist-path`, `checklist-fresh`, `checklist-add` (`#/items/new?job=<id>`), `checklist-empty`, `checklist-done`. Status goes through `api.updateStage(id, { status })`.
- `#/notifications?tab=activity&job=<jobId>` (`src/screens/Notifications.tsx`, `notifications`): `notifications-list`, `notifications-unread` ("2 new" / "Nothing new"), `notification-<id>` (a link; `data-read`), `notifications-mark-all`, `notifications-empty`; phone tabs `notifications-tabs`, `notifications-tab-mine`, `notifications-tab-activity`; the feed `activity`, `activity-<id>` (`data-kind`), `activity-filter-all`, `activity-filter-<jobId>`, `activity-empty`. Desktop shows both panes side by side.
- `src/components/Buzz.tsx`: `<Buzz layout />` is mounted once by the shell; `notificationHref(n, api)` and `notificationLinkWords(n, api)` are the one place a notification is turned into a link (item, step, shipment, else job, else the list; falls back to the job for a role without the item screen). Test ids `buzz` (`data-kind`), `buzz-link`, `buzz-dismiss`, `buzz-more`. Hides after `BUZZ_MS` (8s).
- `fireRemindersDueToday` raises, per person, kind, thing and day: `act_by_today`, `overdue` (late, or act-by passed while still to do), `hold_point_week_away` (with the readiness words), `photos_uploaded` (photos landed today in a required category of an open hold point), `job_unconfirmed` (amber build jobs, to admins and partners). `eta_moved` comes from `setShipmentEta`. Notification and activity text never contains money.

## Hold-point check and daily notes (Stage 5, part A)

- `<HoldPointCheck check refusal? queuedCount? queuedByCategory? uploadHref? addHref? compact? canComplete? testId />`: `addHref(categoryId)` draws "Add photos" on each empty required set (`holdpoint-add-<categoryId>`), `queuedByCategory` draws "2 waiting to send, they don't count yet" per set (`holdpoint-queued-<categoryId>`), `holdpoint-ready` appears when every set has a photo. The refusal renders first, under the caller's Mark done button. `api.holdPointReadiness(stepId)` gives `{ filled, total, words, missingCategories, ok, forecastStart, stepName, jobName, check }` for notifications; `holdPointReadinessWords(check)` in `src/domain/forecast.ts` is the one wording.
- Upload: `#/jobs/:id/upload?stage=&category=&return=<path>&item=<itemId>`. `return` is where Done and the back link go; `item` attaches the first photo that lands to that item (`photoId`, only when it has none).
- Notes: `DailyNote` carries optional `weather`, `onSite` (person and trade ids), `photoIds`, `queued`. `addDailyNote(input)` takes them; `updateDailyNote(id, string | patch)`; `flushDailyNotes()` clears `queued` while there is signal (the notes screen calls it on mount and when `offline` turns false). Screen `#/jobs/:id/notes` (`notes`): `note-today`, `note-text`, `note-weather-<fine|overcast|rain|wind|hot>`, `note-onsite-<personId|tradeId>`, `note-more-trades`, `note-add-photo`, `note-save`, `note-problem`, `note-saved`, `note-queued`, `notes-list`, `notes-week-<monday>`, `note-<id>`, `notes-empty`. `<NoteEntry>` (`src/components/NoteEntry.tsx`) is the form; `WEATHER` and `weatherLabel(key)` are exported for anyone drawing a note.

## Templates and new job (Stage 6, part B)

- Rule 8: a template is a job with `isTemplate` and no dates. `listTemplates()` lists them; `listJobs()` never does; `getForecast(templateId)` is undefined. The stage, step, link, requirement and photo-category methods all work on a template (`addStep` drops `plannedStart` on one).
- `api.previewTemplate(templateId, { startDate, startsFromStageId? })` returns `{ templateId, plannedFinish?, startsOn, stageCount, stagesDone, stepCount }` without saving: the template's durations run forward over working days from the start date through the links (a weekend or shutdown start snaps to `startsOn`), stages before `startsFromStageId` are counted as done, and `plannedFinish` is exactly what `copyTemplate` with the same input would give (both call one `planCopy`). Undefined `plannedFinish` for a design template.
- `api.copyTemplate(templateId, { name, sideId?, path?, startDate, weeklyHoldingCost?, startsFromStageId? })` makes the job (stages, steps, links, requirements, photo categories; earlier stages and their steps done with planned start = end = the working day before the start; a General category if the template has none; a design template gives checklist stages only) and stamps `lastConfirmed = today`. `api.saveJobAsTemplate(jobId, name)` copies a job's program into a new template with every date and status stripped (design jobs: stages only). `NewJobInput.isTemplate` makes a blank template through `addJob` (a design one still gets the DA/CDC checklist stages).
- Routes: `#/templates` (`src/screens/Templates.tsx`, `templates`), `#/templates/:id` (`TemplateDetail`, `template`; Not found unless the job is a template), `#/jobs/new?template=<id>` (`src/screens/NewJob.tsx`, `newjob`). All three use the `templates` screen key (admin and partner); JobsList's "New job" (`jobs-new`) links to `#/jobs/new`. `e2e/no-money-for-site.spec.ts` fills `/templates/:id` with `tpl-duplex`.
- List ids: `templates-new-job`, `template-<id>` (row), `template-open-<id>`, `template-counts-<id>` ("8 stages, 29 steps, 25 needs, 13 photo sets"), `template-use-<id>`, `template-new` + form `template-new-name|kind-<kind>|path-<path>|save|cancel`, `template-save-from-job` + form `template-from-job-<jobId>|name|save|cancel`, `templates-empty`. Detail ids: `template-use`, `template-editor` (part A's `#/jobs/:id/edit`), `template-offline`, `template-empty`, `template-stage-<stageId>`, `template-stage-name-<stageId>` (input), `template-stage-delete-<stageId>`, `template-step-<stepId>`, `template-step-name-<stepId>`, `template-step-days-<stepId>` (wraps an input; Enter or blur saves), `template-step-waits-<stepId>`, `template-step-add-wait-<stepId>` (select), `template-step-needs-<stepId>`, `template-step-need-toggle-<stepId>`, `template-step-add-need-<stepId>`, `template-step-hold-<stepId>` (checkbox), `template-step-delete-<stepId>`, `template-add-step-<stageId>` with `template-add-step-name|days|save-<stageId>`, `template-cats-<stageId|general>`, `template-cat-<catId>`, `template-add-cat-<stageId|general>`, `template-add-stage` with `template-add-stage-name|save`. Offline: admin edits are hidden and `template-offline` says why. Exports: `plural`, `countWords(job, shape)`, `TemplateShape`.
- New job ids: `newjob-name`, `newjob-side-<sideId>` (only when the person belongs to more than one side, else `newjob-side-name`), `newjob-kind-<build|design>`, `newjob-path-<DA|CDC>`, `newjob-holding` (number input, "$ ... /wk"), `newjob-template-<id>` (`aria-pressed`), `newjob-template-blank` (design only: the standard checklist through `addJob`), `newjob-no-templates`, `newjob-start` (date input, default next Monday), `newjob-from-stage-<templateStageId>` (`aria-pressed`, `data-done` on stages that will be marked done; the first stage means "from the start"), `newjob-planned-finish` (the hero panel; `newjob-planned-finish-date` holds "Fri 4 Jun 2027"; design jobs get a sentence instead), `newjob-problems` (words list), `newjob-create` ("Create <name>", disabled until the problems are gone or while offline), `newjob-cancel`. Create opens `#/jobs/<newId>`; a job made for the other side switches the session to that side first. Seed check: the duplex template from Mon 5 Oct 2026 gives Fri 4 Jun 2027; from Lock-up, Tue 2 Mar 2027 with 4 stages done.

## Program editor (Stage 6, part A)

- `#/jobs/:id/edit` (`src/screens/ProgramEditor.tsx`, `editor`, `data-layout` desktop | phone) is desktop-only for admin and partners; the phone renders `editor-phone` with `editor-program-link` to the read-only program. It holds a `ProgramDraft` (`src/data/api.ts`: stages, steps, links, requirements, photoCategories) and writes nothing until Save.
- `api.previewProgramChange(jobId, draft)` returns `ProgramPreview { jobId, finishBefore, finishAfter, deltaDays, costDelta (money), forecast }`: the forecast over the draft against the saved program, planned ends re-derived from planned starts over working days. Undefined for templates and design jobs. Writes nothing. The editor draws `<Gantt forecast={preview.forecast} steps={draft.steps} onSelectStep renderBar />` so every edit redraws the chart.
- `src/components/editor/programDraft.ts`: `readProgram(api, jobId)` (the saved program), `diffProgram(saved, draft)` (the changes in save order), `applyProgramDraft(api, jobId, saved, draft)` (adds first so `draft-` ids get real ids, then edits, removals, stage deletes last; every change goes through the existing add/update/delete methods, each logging its own activity line; returns the draft-to-real id map), `cyclePath(links, stepId, waitsForId)` (the chain that would loop, else null) and `cycleWords(path, stepId, names)`, `makeStage`, `makeStep(draft, stage, { today, dated })` (planned start = the working day after the stage's latest planned end, else the job's, else today; 5 days), `nextPlannedStart`, `withPlannedEnd(step)`, `draftId(kind)`, `isDraftId`.
- Panel pieces: `<StepForm>`, `<StageForm bare>` (design jobs: name and order only), `<LinkPicker>`, `<RequirementList>`, `<CategoryList>`, `<ConfirmDelete>`; a stage-level job's placeholder step opens as the stage (`data-kind="stage-step"`, name renames the stage, step keeps "<stage>, whole stage").
- Test ids: `editor`, `editor-stages`, `editor-stage-<stageId>` (chips, `aria-pressed`), `editor-add-stage`, `editor-step-<stepId>` (the bar wrapper on the chart or the row in a template's `editor-tree`, `data-selected`), `editor-panel` (`data-kind` step | stage | stage-step | none), `editor-name`, `editor-step-stage`, `editor-duration`, `editor-planned-start`, `editor-derived`, `editor-holdpoint`, `editor-trade`, `editor-waits`, `editor-waits-<stepId>`, `editor-waits-remove-<stepId>`, `editor-waits-pick`, `editor-waits-add`, `editor-waits-refusal`, `editor-requirements`, `editor-requirement-add`, `editor-requirement-<id>`, `editor-requirement-kind|name|lead|remove-<id>`, `editor-categories`, `editor-category-add`, `editor-category-<id>`, `editor-category-name|required|remove-<id>`, `editor-add-step`, `editor-move-up`, `editor-move-down`, `editor-delete`, `editor-delete-confirm-box`, `editor-delete-confirm`, `editor-delete-keep`, `editor-foot`, `editor-preview-finish`, `editor-preview-slip`, `editor-changes`, `editor-cancel`, `editor-save`, `editor-offline`.

## Setup screens (Stage 6, part C)

- Notification preferences (UI_PLAN 3.19): `api.getNotificationPrefs(personId?)` returns `{ reminders, hold_points, eta_changes, unconfirmed_jobs }` (`NotificationPrefKey`, `NOTIFICATION_PREF_KEYS`), every key defaulting to the person's `notificationsEnabled` flag; `api.setNotificationPref(key, enabled, personId?)` stores one key per person (the mock keeps them on its store, not on `Person`). `unconfirmed_jobs` is offered to admin and partners only, matching who `fireRemindersDueToday` sends `job_unconfirmed` to.
- Push (model gap 6): `api.savePushSubscription({ device, subscription, enabled? })` upserts one `push_subscription` row per person and device for the current person, sets `person.notificationsEnabled`, and logs "Dominic allowed notifications on iPhone, Safari"; `api.listPushSubscriptions(personId?)` is per person across sides. The prototype stores the words `placeholder: no push server in the prototype` and says so on the screen. `deviceWords()` ("iPhone, Safari") and `detectPlatform()` live in `src/components/InstallSteps.tsx`.
- `api.getLastSync()`: the `YYYY-MM-DDTHH:mm` stamp of the last write through the API (set on every `commit` and on `reset`; undefined before any write). Settings shows it as "Last change saved Thu 3:10pm".
- `<InstallSteps installed? platform? testId? />`: iPhone and Android steps in words; `isStandalone()` (`display-mode: standalone` or `navigator.standalone`) makes it read "Installed". Test ids `settings-install` (`data-installed`), `settings-install-iphone|android`, `settings-install-steps`.
- Settings `#/settings` (`settings`): `settings-who`, `settings-sign-out` (no login: drops to the default person and `#/sign-in`), `settings-steps`, `settings-step-home`, `settings-permission-state` (words per platform from `permissionWords(state, platform)`; on a desktop browser "Notifications aren't set up on a desktop browser in this prototype" with the ask off; blocked shows the unblock words with the ask off; the live state comes from `navigator.permissions.query`), `settings-allow-notifications` (a real tap; "Record this phone" when already allowed but not yet stored), `settings-push-note`, `settings-step-buzz`, `settings-test-buzz`, `settings-pref-<key>` (`role="switch"`, `aria-checked`, "On"/"Off"), `settings-version`, `settings-last-sync`, `settings-reset` → `settings-reset-words`, `settings-reset-confirm`, `settings-reset-keep`, `settings-reset-done` (calls `api.reset`, which also clears the photo queue).
- Trades `#/trades` (`trades`, A P B; all three add, edit and delete): `trade-add`, `trade-add-empty`, `trades-empty` ("No trades yet. Add them as you book them."), `trades-offline` ("Needs signal": every write control disabled offline), `trade-<id>` (row), `trade-open-<id>` (link to the trade), `trade-ring-<id>` (`tel:`; the 56px "Ring" target on the phone), `trade-jobs-<id>` (jobs with an open item waiting on the trade or a trade requirement of its type), `trade-edit-<id>`; the form `trade-add-form` / `trade-edit-form-<id>`: `trade-name`, `trade-type-<type-slug>` (buttons from the side's trade requirements, templates included, unioned with `COMMON_TRADE_TYPES`), `trade-type-other` + `trade-type-other-text`, `trade-phone`, `trade-save`, `trade-cancel`, `trade-delete-<id>` → `trade-delete-words-<id>`, `trade-delete-confirm-<id>`, `trade-delete-keep-<id>`. `telHref()` and `splitTradeTypes()` are exported.
- One trade `#/trades/:id` (`trade-detail`, same access): the number, the jobs, edit and delete through the same form, and the open items waiting on the trade as `<ItemRow testId="trade-detail-item-<id>">` with the job name as context (`trade-detail-items`, `trade-detail-empty`). Version two's "what's owed" goes here.
- People `#/people` (`people`, admin only): rows are the current side's members; `person-<id>` (`data-role`), `person-role-<id>-<admin|partner|builder|site>` (`aria-pressed`; sets the membership on the current side), `person-remove-<id>`, `person-setup-<id>` (screen 19's state in words), `people-refusal` (`roleChangeRefusal`: the acting admin cannot demote or remove themselves), `people-offline` ("Needs signal"), `people-role-words` (what each role sees, including "Never sees prices"); add form `person-add` → `person-add-form`, `person-add-name`, `person-add-phone`, `person-add-role-<role>`, `person-add-side-<sideId>`, `person-add-save`, `person-add-cancel`, `person-added`.
