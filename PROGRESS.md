# PROGRESS

## Stage checklist

- [x] Stage 0: types, seed data, calculator, unit tests, data layer, dev bar. Reviewer sign-off: done
- [x] Stage 1: shell, per-role nav, jobs list, Monday screen, Why it moved. Reviewer sign-off: done
- [x] Stage 2: shipments + ETA impact preview, Gantt, look-ahead, step detail, build job overview. Reviewer sign-off: done
- [x] Stage 3: photo upload + IndexedDB queue, upload queue, gallery, Alec's Today. Reviewer sign-off: done
- [x] Stage 4: waiting-on list, item sheet, call list. Reviewer sign-off: done
- [ ] Stage 5: hold-point check, daily notes, design checklist, notifications + activity. Reviewer sign-off: pending
- [ ] Stage 6: program editor, templates + new job, trades, people + roles, my settings. Reviewer sign-off: pending
- [ ] Deployed to GitHub Pages, live URL confirmed

## Decisions

- 2026-09-18 Orchestrator: repo created private, then flipped to public because the free plan refuses Pages on private repos (data is invented).
- 2026-09-18 Orchestrator: phone breakpoint 768px; tests drive the dev bar via URL params `as` and `today`.
- 2026-09-18 Stage 0: libraries: react-router-dom 6 (HashRouter), vitest 2, @playwright/test 1.63, no UI kit, no icon library, no web fonts (system stack for UI, a condensed system face for hero numbers so the app works offline).
- 2026-09-18 Stage 0: the API (`src/data/api.ts`) is synchronous over a local copy of the data and returns copies; `subscribe` fires on every change. A server adapter keeps the copy warm and refreshes quietly, which is the UI Plan's loading rule. Photo queue methods are async (IndexedDB, hand-rolled, memory fallback in tests).
- 2026-09-18 Stage 0: money fields are named in `MONEY_FIELDS` (`weeklyHoldingCost`, `slipCost`, `slipSincePlanCost`, `costDelta`, `slipCostAfter`); every read for the site role is deep-copied with those keys deleted. Alec's `listJobs` returns builds only and `listItems` returns material items only.
- 2026-09-18 Stage 0: step end is inclusive (`start + duration - 1` working days); a successor starts the next working day; an item's expected date snaps forward to a working day before it can start a step. Done steps use actual dates when recorded, else planned. Nothing is clamped to today.
- 2026-09-18 Stage 0: rule 1 reads "without that item's own expected date" as also excluding items on the same shipment, otherwise the three windows items would hide each other's lateness. Flow d needs all three to read "14 days late".
- 2026-09-18 Stage 0: freshness is amber when days unconfirmed > 7 (8 or more); a job never confirmed is amber with "Never confirmed".
- 2026-09-18 Stage 0: last Monday's snapshot is the one dated `lastMonday(today)`, falling back to the latest snapshot on or before today; slip is undefined with no snapshot. Snapshots saved by the app also carry per-step starts and ends so "why it moved" can compare against last Monday; seeded Seaview and Beatty snapshots carry only the finish, so their explanation runs against the plan.
- 2026-09-18 Stage 0: Park Rd durations (working days): site setup 5, excavation 10, under-slab plumbing 5, formwork 5, slab inspection 1, pour 1, frame 15, frame inspection 1, trusses 5, roof cover 10, fascia 10, brickwork 7, roof plumbing 4 (in progress from 14 Sep), cladding 15 (from 16 Sep), install windows 10 (2 Nov), external doors 5, stormwater 15 (21 Sep), stormwater inspection 1, landscaping 10 (8 Feb 2027), rough-in 10, insulation 4, plasterboard 8 (11 Dec, crosses the shutdown), waterproofing 3, tiling 10, kitchen 10, painting 5, fit-off 3, final inspection 1, handover 1 (Fri 26 Feb 2027). The chain from Install windows to Handover has zero slack so 16 Nov gives 12 Mar exactly.
- 2026-09-18 Stage 0: Seaview's Slab stage has three steps (prep, "Slab inspection before pour" hold point Mon 28 Sep, "Pour ground floor slab" Fri 2 Oct) rather than one placeholder, because flow e needs the hold point and "Book concrete pump" needs a Friday needed-by for a 2-week lead to land on Fri 18 Sep. Flow c's "for: Pour ground floor slab, 28 Sep" therefore reads 2 Oct; 28 Sep is the inspection.
- 2026-09-18 Stage 0: Seaview placeholder durations 15/40/15/30/135/40/5 land Handover on Fri 29 Oct 2027 (see open problems).
- 2026-09-18 Stage 0: Beatty placeholder durations 10/15/15/10/30/5 give planned finish Fri 27 Nov; the tiler expected Mon 5 Oct moves it to Fri 4 Dec. The 14 Sep snapshot is seeded at Sun 29 Nov so slip reads exactly +5 days, $1,430 (see open problems).
- 2026-09-18 Stage 0: design jobs use typed `neededBy`; "oldest N days" is days since `item.createdAt`. West St items created 25 Aug and 3 Sep, Tollbar 9 Sep, John St 13 Sep.
- 2026-09-18 Stage 0: `copyTemplate` copies stages, steps, links, requirements and photo categories; stages before "starts from" are done with planned start = end = the working day before the start date; the rest run forward from the start date through the links. Template stage statuses are reset to not started.
- 2026-09-18 Stage 0: Park Rd "Windows installed" category is not flagged required because Lock-up has no hold point; Slab, Frame, External works and Handover categories are.
- 2026-09-18 Stage 0: `fireRemindersDueToday` raises act-by-today (owner, status to do), overdue (owner), and hold-point-a-week-away-with-photos-missing (builders and admins on the side), deduplicated per person, kind, item/step and day. ETA changes raise "X now expected D. N of your items moved." to each owner of a linked open item.
- 2026-09-18 Stage 0: Raff's call-list cut (act-by within 14 days or past, status to do or booked) is 9 items across 3 jobs in the seed, matching flow c.
- 2026-09-18 Stage 0: service worker parses the built index.html for `/assets/` URLs at install; navigation is network-first with the cached shell as fallback, hashed assets cache-first; registered only in production builds.
- 2026-09-18 Orchestrator (locked, A): Beatty keeps SPEC's numbers (finish Fri 4 Dec 2026, slip +5, $1,430); its 14 Sep snapshot stays a stored value of 29 Nov. "Why it moved" on Beatty labels both figures: "Finish 4 Dec, 7 days later than planned (27 Nov)" and "Finish 4 Dec, 5 days later than Monday's snapshot (29 Nov)"; finish lines always name their baseline, never a bare "+7 days". The Beatty "unconfirmed 7 days" notification now has its own kind, `job_unconfirmed`.
- 2026-09-18 Orchestrator (locked, B): Seaview's forecast finish is Fri 29 Oct 2027 (30 Oct is a Saturday); `job.plannedFinish` is 2027-10-29 too, SPEC.md's mock-data line says so, and the test asserts 2027-10-29.
- 2026-09-18 Orchestrator (locked, C): flow c's "Pour ground floor slab" is Fri 2 Oct in the seed (28 Sep is the slab inspection); Stage 4 renders the date from data. No seed change.
- 2026-09-18 Orchestrator (locked, D): SPEC.md rule 5 now says public holidays other than the shutdown are out of scope for the prototype.
- 2026-09-18 Stage 0 fixes: tests split for purity: `src/domain/forecast.test.ts` tests the rules on a hand-built job and imports nothing outside `src/domain`; SPEC's mock-data numbers live in `src/seed/seed.test.ts` (plus holding costs, the 12-week windows lead, phones on every trade, a week of Park Rd notes, data-URL photos, the empty Norm side); the data layer and photo queue are in `src/data/mockApi.test.ts`.
- 2026-09-18 Stage 0 fixes: the service worker caches a navigation response as the shell only when `res.ok`.
- 2026-09-18 Stage 0 fixes: dev bar "Reset" reseeds the data, clears the photo queue, and sets today back to 2026-09-17 and offline to false, keeping the current person. The smoke spec covers it.
- 2026-09-18 Stage 0: Playwright "phone" project is Desktop Chrome at 390x844 with `isMobile` and touch (no WebKit download); CI does not run Playwright.
- 2026-09-18 Stage 1 (Monday): `<Money>` returns null when `value === undefined`, label included; `<SlipText>` puts the cost through it, so a stripped row reads "+14 days" alone. Slip 0 renders "0" (never "+0 days") with the label "Nothing moved"; undefined renders a dash and "Slip appears after the first Monday".
- 2026-09-18 Stage 1 (Monday): `<BigNumber>` has two sizes: "hero" (44/56px) for a screen's one big figure and "row" (28/32px) so a column of finish dates leads a six-column table without crowding it. Label is small sentence-case words under the figure; the table hides the in-cell label because the column header already says it.
- 2026-09-18 Stage 1 (Monday): desktop table vs phone cards is a `matchMedia('(min-width: 768px)')` hook in Monday.tsx, one set of cell components for both, so every test id exists exactly once at either size.
- 2026-09-18 Stage 1 (Monday): the forecast finish shows in full ("Fri 26 Feb 2027") on both layouts rather than the wireframe's "12 Mar 27"; the weekday is what partners say out loud and the flow spec asserts the long form.
- 2026-09-18 Stage 1 (Monday): the slip figure is the link to "Why it moved" (label under the figure; "Nothing moved" when slip is 0, still linked so the page confirms it); the job name links to the job; each waiting-on line links to its item. "!" precedes late items and amber freshness so the screen reads in greyscale. No totals row: UI_PLAN 3.2 and the wireframe list none.
- 2026-09-18 Stage 1 (Monday): the "since Monday / since the original plan" toggle is the `?since=plan` hash query, carried into `#/jobs/:id/why?since=plan`, so a reloaded page keeps it and the Why screen opens on the same baseline.
- 2026-09-18 Stage 1 (Why it moved): entries render in forecast order as a numbered chain (cause, step, stage, finish); `refId` links to `#/items/:id` (or `#/shipments/:id` when the ref is a shipment, checked via `api.getItem`), `#/steps/:id`, and `#/jobs/:id/program` for stages. When Monday's snapshot holds only the finish (Seaview, Beatty), a line under the chain title says the steps are read against the plan and the last line gives the slip since Monday.
- 2026-09-18 Stage 1 (flow a spec): Park Rd's slip is 0 in the seed (the ETA change is flow d), so the spec asserts Beatty as the top row and opens "Why it moved" on Beatty; Alec's Monday visit asserts no rows and no "$" only, because the shell decides the wording of the access message.
- 2026-09-18 Stage 1 (shell): routes live in one registry (`src/shell/routes.ts`: path, screen key, title, stage); App.tsx maps it to <Route>s, unbuilt ones render `src/screens/Placeholder.tsx` ("Coming in stage N"), and `<Guard>` checks `api.canSee` per route. A refused route shows "You don't have access to this"; `#/monday` as Alec falls through to Not found, as UI_PLAN section 2 asks. Sign in is a stub at `#/sign-in`; `#/` is the landing redirect.
- 2026-09-18 Stage 1 (shell): the landing redirect waits until the session matches the hash params (`?as=`, `side`, `today`, `offline`) before navigating, because the provider applies them in an effect after the first render; otherwise `#/?as=raff` would redirect the previous person. Homes: admin and partners `#/monday`, builder `#/waiting?owner=me`, site `#/jobs/<last opened build job, else the first>` (the shell remembers the last opened build job in localStorage `construction-tracker.lastJob.v1`).
- 2026-09-18 Stage 1 (shell): layout is a `matchMedia` hook plus the role: below 768px, and always for the site role, the phone chrome renders (top bar with side switcher or side name, bell, person; bottom tab bar, text labels, 56px targets); otherwise the desktop sidebar. Screens read `useLayout()` from AppShell to switch cards and tables, so every test id exists once. Alec on a wide window gets the phone column centred at 480px with hairline edges.
- 2026-09-18 Stage 1 (shell): nav lists come from `api.canSee`, in UI_PLAN's order: phone tabs per role exactly as section 2; desktop main group Monday, Waiting on, Call list, Jobs (with every job nested), Shipments, Activity; Setup group Templates and new job, Trades, People and roles filtered by role (so Raff sees Trades under Setup). "Activity" opens `#/notifications`, where the plan puts the feed as a tab. Test ids `nav-<name>` (`nav-today`, `nav-waiting` for both "My items" and "Waiting on", `nav-upload` for "+ Photos", `nav-notifications`, `nav-settings`, `nav-job-<id>`); the tab bar and the sidebar main list are `primary-nav`, the setup group `setup-nav`.
- 2026-09-18 Stage 1 (shell): "you are here" is weight and a concrete wash, never orange; hi-vis is spent on primary actions only (New job). The two icons (bell, person) are hand-drawn inline SVG, no icon library; the unread count is a number with sr-only "unread". The side switcher is a native select (`side-switcher`); people on one side see the side name as text (`side-name`).
- 2026-09-18 Stage 1 (shell): the offline bar is one amber sentence under the header, "No signal. Showing what loaded at 1:09am. Photos, notes and ticks wait in the queue."; the time is the wall clock captured when offline turned on (not the session date, which is a data date). No modal, no red.
- 2026-09-18 Stage 1 (shell): the sticky sidebar is sized to the viewport minus the dev bar (measured with a ResizeObserver into `--above`) so its foot (bell, me) stays on screen at 800px tall; links are 36px, nested jobs 28px.
- 2026-09-18 Stage 1 (jobs list): columns Job, Stage, Forecast finish (date in the condensed face plus "7 days late" / "On plan" / "N days early"), Slip ("+5 days this week", "No change this week", "Slip appears after the first Monday"), Holding cost (`<Money suffix="/wk">`; the column only exists when some row carries the field, so Alec's table has no empty column), Last confirmed ("Confirmed 2 days ago", amber "Unconfirmed 9 days" after 7 days, amber "Never confirmed"). Design rows: Job with DA/CDC, Stage, Outstanding ("2 outstanding, oldest 23 days", amber only once the oldest passes 14 days, per the wireframe's plain text), Last confirmed. "New job" goes to `#/templates` (Stage 6). Whole rows are targets (`job-row-<id>`), with the name a real link for keyboards.
- 2026-09-18 Stage 1 (StatusText): `src/components/StatusText.tsx` renders words on a wash (late, amber, ok, muted, plain); late adds a leading "!" so the page reads in greyscale. Freshness on phone cards sits on its own line under the finish and slip chips so three chips never wrap raggedly.
- 2026-09-18 Stage 1 (shell): `e2e/smoke.spec.ts` (Stage 0's heading) is folded into `e2e/shell.spec.ts` (dev bar checks kept) and removed; `src/styles/app.css` is emptied of the Stage 0 classes, shared page, header and button styles live in `src/shell/shell.css`.
- 2026-09-18 Stage 1 review fixes (Monday): the builds table shrinks the job, finish, slip and holding columns to their content and gives the room to waiting-on; the freshness cell wraps instead of widening the table, and the flow a spec asserts `scrollWidth <= innerWidth` on the desktop project (as a string expression, since the e2e tsconfig has no DOM lib).
- 2026-09-18 Stage 1 review fixes (Monday): the whole row and the whole phone card open `#/jobs/:id` on click; links inside (job name, slip figure to Why it moved, items) keep their own targets via a `closest('a, button')` check rather than stopPropagation on each. Keyboard users reach the same places through the job-name link.
- 2026-09-18 Stage 1 review fixes (Monday): phone cards pass `label="Holding"` to `<Money>` ("Holding $2,000/wk"); the desktop table keeps the column header as the label. Money still renders nothing when the value is absent.
- 2026-09-18 Stage 1 review fixes (Monday, Why): screen titles drop to 22px so the 28px row figures and the 44/56px hero are the largest type. Freshness renders through the shared `<StatusText tone="amber"|"muted" testId>`, which prefixes "!" for late and amber, so Monday and Jobs read the same; Monday no longer hand-rolls the mark.
- 2026-09-18 Stage 1 review fixes (shell): contrast: DA/CDC and sidebar "design" labels are `--text-muted` (5.0:1); the hi-vis button is dark ink on orange (5.2:1, the vest) rather than white (3.0:1); the sidebar is 260px and job names wrap instead of truncating.
- 2026-09-18 Stage 1 review fixes (shell): `src/shell/routes.tsx` carries the element per route (one-place edit; the `elementFor` switch and unused `BUILT` are gone). `e2e/no-money-for-site.spec.ts` reads the `{ path: '...'` entries from that file with a regex (Playwright cannot import a module that imports CSS), substitutes seed ids (park-rd, pr-install-windows, it-pr-windows, sh-park-windows), visits each as Alec in both projects and asserts no "$", "holding" or "slip cost" in the body text and no leaf element anywhere in the DOM containing "$".
- 2026-09-18 Stage 1 review fixes (jobs list): freshness comes from `getForecast(jobId).freshness` only; the amber case is reworded "Unconfirmed N days", otherwise the calculator's "Last confirmed 2 days ago" is shown as is.
- 2026-09-18 Stage 1 review fixes (StatusText, locked): "!" prefixes both late and amber; the prop is `testId`, like every shared component. CONTRACTS.md has a "Shell and shared components" section.
- 2026-09-18 Stage 2 (shipments): list rows are `shipment-row-<id>`; timing is words from `timing(eta, neededBy)` in Shipments.tsx: "ETA 1 week before needed" (ok, plain on desktop), "ETA on the day it is needed" (amber), "ETA 2 weeks after needed" (late); whole weeks read as weeks, the rest as days. Needed-by is the earliest `neededBy` among open linked items, from the calculator. Rows sort by ETA.
- 2026-09-18 Stage 2 (shipments): the list's one money figure is the job's weekly holding cost under the header "A week late costs" (phone card: `<Money label="A week late costs">`), so a partner can price a slipping ETA on sight; the column only exists when some row carries the field. No "Add shipment" form yet: the task listed none and the seed has one shipment; reported.
- 2026-09-18 Stage 2 (shipments): shipment detail's hero is the ETA (`shipment-eta`, BigNumber hero, late tone only when the words say late). Status is four buttons in a row (`shipment-status-<status>`, aria-pressed on the current one, steel fill so it reads without colour, a dot per UI_PLAN's "four dots"), never a dropdown. Marking Delivered offers "Mark the 3 linked items done?" (`shipment-offer-done`, `shipment-mark-items-done`), which runs `updateItemStatus(id, 'done')` per open item.
- 2026-09-18 Stage 2 (shipments): the ETA editor is a native date input (`shipment-eta-input`); as soon as the draft differs from the saved ETA the impact panel (`src/components/EtaImpact.tsx`, `shipment-eta-preview`) appears from `api.previewEtaChange`: "3 linked items would be expected Mon 16 Nov", "Install windows would start Mon 16 Nov, not Mon 2 Nov, and 11 steps after it move with it", then "<job> finish" as a row BigNumber (`shipment-eta-preview-finish`) with `<SlipText days={deltaDays} cost={costDelta}>` (`shipment-eta-preview-slip`) and "was Fri 26 Feb". Conditional wording ("would") because nothing is saved. When slip since Monday would differ from the delta (a job already slipped this week) a quiet last line gives it. The panel is timber paper: a note before the change is made.
- 2026-09-18 Stage 2 (shipments): "Save new ETA" (`shipment-save-eta`, the screen's one hi-vis button) calls `api.setShipmentEta`, which already logs `eta_changed` (from, to) and raises "now expected" notifications to owners; nothing added to mockApi. "Keep 26 Oct" (`shipment-cancel-eta`) drops the draft. Linked items' expected dates come from `forecast.items[id].expected` (`expectedFromShipmentId`), not recomputed. Offline: the input and the status buttons are disabled with "Needs signal" (UI_PLAN 3.13: read-only).
- 2026-09-18 Stage 2 (shipments): History lists `listActivity({ shipmentId })` (ETA changes and status moves), "12 Aug 10:05am  text (Dominic)" using `shortName`, newest first; the flow d spec asserts the new entry lands on top. Alec is refused at the guard (shipments are A P B), so his shipment screens are the shared access message and no money question arises.
- 2026-09-18 Stage 2 (flow d spec): `e2e/flow-d-eta-moves-finish.spec.ts` follows UI_PLAN flow d steps 1 to 8 as Dominic at both projects, asserts Monday's Park Rd row (Fri 12 Mar 2027, +14 days, $9,000) and "Why it moved" (`why-entry-1` names the ETA change, `why-link-sh-park-windows`), then resets through `dev-reset`; two more tests cover status buttons plus the Delivered offer (as Raff) and the offline lock.
- 2026-09-18 Stage 2 (overview/step): `src/screens/JobOverview.tsx` is the build overview for every role; `JobRoute` in routes.tsx now only sends design jobs to the Stage 5 placeholder. Alec's version is the same screen with a "Today" placeholder section (`job-today-placeholder`, Stage 3 replaces it), tabs Today/Program/Photos/Deliveries, the finish date and the next hold point; no slip, facts or waiting-on section (his items are deliveries, Stage 3). Money still goes through `<Money>` only.
- 2026-09-18 Stage 2 (overview/step): late vs plan is worded "7 days late, planned Fri 27 Nov" / "On plan" / "N days early" (`job-late`); the hero is the finish date in the condensed face, late-toned only when late. The facts row is slip (`job-slip`, the figure links to Why it moved with "Nothing moved" / "Why it moved" under it), holding cost, and "Program: Last confirmed N days ago" with the "Confirm program" button (`job-confirm`, disabled with "Needs signal" offline; `api.confirmJob` stamps today and the calculator's freshness words update).
- 2026-09-18 Stage 2 (overview/step): the job tabs (Overview, Program, Waiting on `#/waiting?job=<id>`, Photos, Notes) are drawn by the overview itself (`job__tabs`), not a shared component, so Part B's program view is free to draw its own; a shared JobTabs can be lifted in Stage 3 if the screens agree.
- 2026-09-18 Stage 2 (overview/step): hold-point readiness reads "1 of 3 required photo sets uploaded" (`readinessWords` in `src/components/HoldPointCheck.tsx`); the block lists categories with "4 photos" / "none yet" (`holdpoint-category-<id>`), shows the refusal sentence from `setStepStatus` inline as an alert (`holdpoint-refusal`), names queued photos that do not count yet, and links "Add photos now" to `#/jobs/<id>/upload?stage=<stageId>` (Stage 3 may read the `stage` param). The overview uses it `compact`; the step detail uses it full; Stage 5 extends it.
- 2026-09-18 Stage 2 (overview/step): `src/components/ItemRow.tsx` is the one item row (type word in a fixed 5.5em column, title, "waiting on X, with Raff", one date phrase and a status word). Date phrase order: "Expected 5 Oct, 7 days late" (late) > "Act by Mon 10 Aug, passed" (amber) > "Expected ..." > "Act by ..." (to do) > "Needed ...". Status words: To do, Ordered (material) or Booked, Confirmed, Done. `href` makes it a link; Alec's rows are plain because he has no item screen. Test id `item-row-<id>`.
- 2026-09-18 Stage 2 (overview/step): step detail shows the forecast span as the big figure with the year on both ends ("Mon 2 Nov 2026 to Fri 13 Nov 2026", programs cross the year), planned span, duration in working days, status, and the calculator's `reason` sentence. "Mark started" / "Mark done" (hi-vis) / "Reopen" for builder and above; Done on a hold point is disabled offline with "Needs signal" words, otherwise the button is always live and the refusal is shown in words. Alec reads only.
- 2026-09-18 Stage 2 (overview/step): `e2e/shell.spec.ts` visited `/steps/x`; with a real step screen a missing id is Not found, so that route list now uses `/steps/pr-install-windows`.
- 2026-09-18 Stage 2 (program): the Gantt axis is calendar days (`src/components/gantt/timeScale.ts`, 5px a day, weeks ticked on Mondays, months above), not working days, so a week is always the same width and the shutdown can be drawn as a hatched band; a step that crosses it (Plasterboard) visibly stretches over it. The axis runs from the Monday a week before the earliest planned or forecast date to a week after the latest, and always covers today plus three weeks. The chart opens scrolled so today sits a quarter of the way in.
- 2026-09-18 Stage 2 (program): the forecast bar is a link (`gantt-bar-<id>`, steel-700; done steel-200 with a steel outline; in progress steel-500) drawn over a timber outline at the planned position (`gantt-planned-<id>`, `data-moved`), rendered for every step so the outline is there when a step moves and hidden under the bar while it is on plan. Late steps get "! N days late" in words beside the bar (`gantt-late-<id>`), and stage bands get the same. Hold points are a diamond bar plus a diamond outline and the words "hold point" in the label column.
- 2026-09-18 Stage 2 (program): step links are thin elbows in an SVG overlay behind the bars (predecessor end to successor start), darkened when a bar is hovered or focused; the bar's title and aria-label and the caption under the chart say "Waits for X" in words along with the dates and the late text, so the link is readable without a mouse. The caption is the legend when nothing is pointed at.
- 2026-09-18 Stage 2 (program): only the chart scrolls sideways: `.gantt__scroll` is overflow auto with a sticky label column (292px) and a sticky axis, max height the viewport minus the dev bar and 240px; the spec asserts `scrollWidth <= innerWidth` on the page and `scrollWidth > clientWidth` on the chart. Desktop filters All / Look-ahead / Late only are the `?view=` hash query (`program-view-<key>`); Late only with nothing late reads "Nothing is late. Every step is on its planned dates." "Edit program" (admin, partner) links to `#/jobs/:id/edit` for Stage 6.
- 2026-09-18 Stage 2 (program): stage-level jobs: a stage whose only step is a placeholder is one row with the stage name and the step's bar (no separate band row), so Seaview's Slab (three real steps) keeps its band and the rest read as stage bars. Design jobs get a one-line note and a link to the checklist, no Gantt.
- 2026-09-18 Stage 2 (program): the phone screen is, in order, the stages strip (`stages-strip`, chips `stage-chip-<id>`, current in bold on a concrete wash, tap scrolls to that stage's band), the look-ahead (`lookahead`, `lookahead-week-1..3`: steps starting that week, then steps finishing that week that started earlier; "Nothing starts or finishes." when empty), "Later" naming any late later step as "moved to D, N days late" with the rest behind a details toggle, then the "Stages" list (band per stage: dates, "Done" / "In progress, week 3 of 12" / "Starts D", a thin time-elapsed bar, late steps named under it). Rows are 56px links to the step. A "Full program" button (`program-full-link`, `?view=gantt`) shows the Gantt with 44px rows for anyone who insists.
- 2026-09-18 Stage 2 (program): look-ahead rows say dates in weekdays ("Mon to Thu", "from Wed, 3 wks", "Finishes Fri"), the trade, "started" / "done", then planned vs forecast in words ("on plan, Mon 14 Sep" muted, or "planned Mon 2 Nov, now Mon 16 Nov, 14 days late" on the late wash), then only the unconfirmed or late items it needs, as "needs plumber for stormwater, ordered or booked, not confirmed" (amber) or "needs windows, 14 days late"; the item title loses its leading "Book" / "Order". Confirmed needs are not listed, per UI_PLAN ("any item it needs that isn't confirmed"). StatusText inside look-ahead rows wraps (the shared chip is nowrap), because a 390px screen otherwise scrolled sideways.
- 2026-09-18 Stage 2 (program): the late-step e2e drives the ETA through the shipment screen and accepts either `shipment-save-eta` (CONTRACTS) or `shipment-save-eta` (Part A's current id); it skips if the shipment screen is absent.
- 2026-09-18 Stage 2 review fixes (shipments): ETA test ids follow the `<screen>-<thing>` rule: `shipment-save-eta`, `shipment-cancel-eta`, `shipment-eta-preview`, `shipment-eta-preview-finish`, `shipment-eta-preview-slip` (plus `-items`, `-step`, `-since-monday` lines inside the panel). CONTRACTS.md line updated; `<EtaImpact>`'s default `testId` is `shipment-eta-preview`.
- 2026-09-18 Stage 2 review fixes (shipments): "Add shipment" (`shipment-add`, A P B, disabled offline because an ETA is a date) opens an inline form on the list: name, optional supplier, job and status as pressed buttons (`shipment-add-job-<id>`, `shipment-add-status-<status>`; build jobs only), ETA date, `shipment-add-save` (disabled until name, job and ETA are set) and `shipment-add-cancel`. Saves through the existing `api.addShipment` (no API change) and opens the new shipment so items can be linked there.
- 2026-09-18 Stage 2 review fixes (shipments): "Link an item" under the linked items lists the job's material-to-order items with no shipment (`shipment-link-item-<id>`); every linked row has an "Unlink" text button (`shipment-unlink-item-<id>`, hidden when offline or for a role that cannot edit). Both go through `api.linkItemToShipment`, which logs; the linked item's expected date follows the ETA through the calculator, asserted in the spec ("Order external doors" reads expected Fri 20 Nov after linking to a 20 Nov shipment). Item status words come from `ITEM_STATUS_LABELS` ("Ordered or booked").
- 2026-09-18 Stage 2 review fixes (flow d spec): a desktop-only test adds a shipment, links and unlinks "Order external doors", checks the list shows two shipments, then resets.
- 2026-09-18 Stage 2 review fixes (program): the scroll-to-today effect is keyed on the chart being non-empty and on `view`, since an empty "Late only" unmounts the scroller; the spec asserts `scrollLeft > 0` after Late only (empty) then All. Gantt labels wrap to two lines (row is 36px, 14px text at 1.15) with "hold point" inline after the name; the spec asserts the Slab inspection name is not clipped. Look-ahead rows put the dates on one line and "Roof plumber, started" on the next, no middle dots. "Edit program" renders only on the desktop layout (the editor is desktop-only); the spec checks Dominic on a phone has no `program-edit`.
- 2026-09-18 Stage 2 review fixes (program): Gantt takes optional `onSelectStep(stepId)` (bars become buttons that call back instead of linking) and `renderBar(step, geometry, defaultBar)` (replaces what is drawn in the track; planned outline and late text stay) so the Stage 6 editor reuses it without forking; documented in CONTRACTS.md "Program and Gantt". The late-step spec now uses `shipment-save-eta` only, with no skip.
- 2026-09-18 Orchestrator (locked, E): a step whose forecast start is after today cannot be marked done. `setStepStatus(id, 'done')` returns `{ ok: false, reason: 'not_started', startsOn, missingCategories: [], message: "This step hasn't started yet; it starts Mon 2 Nov." }`; hold-point refusals now carry `reason: 'hold_point'`. The hold-point check runs first, so flow e still reads the category refusal on 17 Sep; ticking Seaview's slab inspection off needs `today` on or after Mon 28 Sep (the mockApi tests set it; the Stage 5 flow e spec must too). Marking a step started early is still allowed.
- 2026-09-18 Stage 2 review fixes (overview/step): phone layout (`.shell--phone`, so always Alec) raises the job tabs, hold-point link, "Open the program", back link and waits-for/holds-up links to 56px; desktop keeps 32/44px.
- 2026-09-18 Stage 2 review fixes (overview/step): `ItemRow` takes an `action` slot rendered outside the link (Stage 4's status advance and Finish call), works out "with you" itself from the session, and words status with `ITEM_STATUS_LABELS` ("Ordered or booked").
- 2026-09-18 Stage 2 review fixes (overview/step): the step detail counts queued photos only in the step's required categories, so "N photos are waiting to upload" is never about another stage. The not-started refusal shows under the buttons (`step-refusal`); the hold-point refusal stays inside the HoldPointCheck block (`holdpoint-refusal`).
- 2026-09-18 Stage 2 review fixes (overview/step): the overview's stage strip is a list in words (`job-stage-<id>`: "5 Oct to 16 Oct, 7 days late" with "planned 28 Sep to 9 Oct" under it when the span moved), not part B's `StagesStrip`, whose chips jump to bands that exist only on the program page. Latest photos on the overview are Stage 3's (gallery); the "N photos" link is the placeholder.
- 2026-09-18 Stage 3 (gallery/today): `src/screens/AlecToday.tsx` renders inside JobOverview for the site role in place of the Stage 2 placeholder (`today`): the day in full words as the display heading ("Thursday 17 September", the screen's one hero; the finish date under it drops to row scale via `.today ~ .job__hero`), "Lock-up stage, week 3 of 12", a full-width 64px hi-vis "Add photos" (`today-add-photos`, `#/jobs/:id/upload?stage=<current>`), "N photos waiting to send" from `listQueuedPhotos` with a clock glyph and a `#/queue` link, the note line (`today-note`, `#/jobs/:id/notes`, shows today's note if one exists), "On site this week" (`today-week`, steps active in Mon to Sun in weekday words, 56px links), "Deliveries this week" (`today-deliveries`, late plus this week's rows), and the next hold point (`today-holdpoint`, readiness words, compact HoldPointCheck with "Add photos now"). The overview's own hold-point section is skipped for Alec so nothing is said twice; `e2e/job-overview-step.spec.ts` now asserts `today` and `today-holdpoint`.
- 2026-09-18 Stage 3 (gallery/today): the overview's `.job__body` two-column grid is scoped to `.shell--desktop`, because Alec's centred phone column on a wide window was squeezing the stages list into half its width (pre-existing).
- 2026-09-18 Stage 3 (gallery/today): `src/screens/Deliveries.tsx` (`deliveries`) lists, across every build job Alec sees, material items that are ordered or confirmed (to-do items are not deliveries yet) plus each job's shipments, grouped Late / This week / Next week / Later / No date yet / Delivered this week (`deliveries-group-<key>`), rows `delivery-<id>` with the job name, the supplier, the contents for a shipment, one date sentence and a status word (`delivery-status-<id>`: Ordered, Confirmed, Shipped, Delivered). Late is words: the calculator's "Expected Fri 18 Sep, 4 days late" (late wash) or "Expected Tue 15 Sep, 2 days ago, not marked delivered" (amber) when the date passed. "Mark delivered" (`delivery-mark-<id>`, 56px, live offline because status ticks queue) runs `updateItemStatus(id, 'done')` or `setShipmentStatus(id, 'delivered')` plus done for its open material items; the row stays under "Delivered this week" for 7 days (`doneAt`) so the word is seen to change. `collectDeliveries`, `groupDeliveries`, `DeliveryRow` are exported for Today. The plan's "Nothing to edit" is overridden by the task's Mark delivered.
- 2026-09-18 Stage 3 (gallery/today): `src/screens/PhotoGallery.tsx` (`gallery`) groups by stage in program order then General (`gallery-stage-<id>`, count words "6 photos, 3 of 3 required sets" in `gallery-stage-count-<id>`), then category (`gallery-category-<id>`: "3 photos" plus "needed for inspection", or "No photos yet" with an amber "Needed before the stormwater inspection" naming the stage's hold-point step and an "Upload here" link `gallery-upload-<catId>` to `#/jobs/:id/upload?stage=&category=`). Stages with neither categories nor photos are not drawn. Filters are chips (`gallery-filter-all|<stageId>|general`, `gallery-filter-by-anyone|<personId>` when more than one uploader), 56px on the phone shell, 40px on the desktop, state in the hash query (`?stage=`, `?by=`, `?sort=`, `?photo=`); the desktop adds a native "Sort" select (`gallery-sort`, newest/oldest). Queued photos for the job are drawn in their category dimmed with "◷ waiting to send" (`gallery-queued-<id>`); they never count. Thumbnails are 3 across on the phone, 6 on the desktop.
- 2026-09-18 Stage 3 (gallery/today): tapping a photo opens a plain full-size panel (`gallery-view`, `?photo=<id>`, Escape or Close closes) with Taken (`gallery-view-taken`), Uploaded who and when (`gallery-view-who`), Filed under stage and category (`gallery-view-category`). Admin and partners get "Move to <category>" buttons for the same stage (`gallery-move-<catId>`, `api.movePhoto`) and "Delete photo" with an inline "Delete it / Keep it" confirm (`gallery-delete`, `gallery-delete-confirm`, `api.deletePhoto`). No "Use as item photo" link: UI_PLAN 3.14 does not ask for one, so none is drawn anywhere.
- 2026-09-18 Stage 3 (gallery/today): the overview's "Latest photos" strip (`overview-latest-photos`, for every role) is the 6 newest thumbnails, each linking to `#/jobs/:id/photos?photo=<id>` (`overview-photo-<id>`) so the gallery opens on that photo, plus "All 20 photos" (`overview-all-photos`). The Stage 2 "20 photos" link in the notes section stays.
- 2026-09-18 Stage 3 (gallery/today): `e2e/gallery-today.spec.ts` covers Today (date, stage, hold point words, Add photos >= 56px by boundingBox, every a/button in the section >= 56px, deliveries this week, Seaview 1 of 3), Deliveries (groups, late words, Mark delivered on an item and a shipment, reset), the gallery as Alec (counts, filter chips, viewer) and as Dominic (20 seeded photos, uploader filter, desktop sort, move, delete, reset), and the overview strip; no "$" as Alec on each. `e2e/shell.spec.ts` now expects `/deliveries` to be the screen, not the placeholder.
- 2026-09-18 Stage 3 (upload/queue): `src/data/photoQueue.ts` gains `subscribe(listener)` (fires after every enqueue, state change, remove, clear), `thumbDataUrl` and `blob` on each entry (the original File kept for the real server; the mock uploads `dataUrl`, downscaled to 800px on the phone because records live in localStorage; the cap moves to 2,000px when the server takes the Blob), `attempts` for the retry back-off, and state names `queued | sending | failed` (was `waiting`). `flush` is single-flight per queue (a second call while one runs gets the same promise, so a photo never lands twice) and re-sends anything left `sending` by a closed app.
- 2026-09-18 Stage 3 (upload/queue): the browser has one queue: `sharedPhotoQueue()`; `createPhotoQueue()` returns it when IndexedDB exists, so the api and the UI (`useQueuedPhotos` in `src/components/QueueBadge.tsx`) watch the same store; without IndexedDB (tests) each api gets its own memory queue as before. `useQueuedPhotos` refreshes on queue changes and on api changes (reset clears the queue through the api).
- 2026-09-18 Stage 3 (upload/queue): the upload screen saves the extras with `api.queuePhoto` followed by `sharedPhotoQueue().update(id, { thumbDataUrl, blob })` rather than widening `NewPhotoInput` (api.ts is not this task's file); the api stays the one write path and the flush uploads `dataUrl` unchanged.
- 2026-09-18 Stage 3 (upload/queue): `<QueueBadge>` is a thin strip under the offline bar on every screen except `/queue` ("3 photos waiting to send", clock glyph, link to the queue, `queue-badge`), hidden at zero; the shell mounts it right after `<OfflineBar />`. The strip also owns the automatic sends because it is always mounted: a flush on the browser's `online` event, and a retry after a failed send with a wait of 5s x attempts, capped at 60s; the provider's flush when the offline flag turns false is unchanged. "No signal" for the screens means the session flag or `navigator.onLine === false` (`noSignal()`).
- 2026-09-18 Stage 3 (upload/queue): photo upload (`src/screens/PhotoUpload.tsx`, `#/jobs/:id/upload?stage=&category=`) is one path on or off signal: every photo is downscaled on a canvas, saved to the queue, then flushed; the line reads "Sending 6 of 11" with a steel bar, "All 11 photos uploaded to Lock-up, Windows installed." when done, or the timber-paper note "No signal: 11 photos saved on this phone, they'll send when you're back in range" with a clock glyph when the signal is off or drops part way (whatever is left of the batch is counted). The button is "Upload N photos"; afterwards "Done" (to the job) and "Add more photos". Failed saves keep the picked photos and say so (`upload-problem`).
- 2026-09-18 Stage 3 (upload/queue): stage and category are buttons (`upload-stage-<id>` chips, 56px, current one marked "now"; `src/components/CategoryPicker.tsx` rows, 56px, chosen one filled steel, count in the condensed face on the right, "none yet" in words, "N waiting to send" when queued). Required categories say "Needed for the <hold point step name>" from `forecast.holdPoints`. The stage's own categories come first, General last; a stage with none shows "Dominic hasn't set up photo categories for X yet. File these under General for now." (`upload-no-categories`). Two file inputs: `upload-file-input` (multiple, camera roll) and, on the phone layout only, `upload-camera-input` with `capture="environment"` ("Take a photo"), because `capture` on the multiple input would open the camera instead of the roll.
- 2026-09-18 Stage 3 (upload/queue): upload queue (`src/screens/UploadQueue.tsx`) groups by job then "Stage: Category", rows are thumbnail, "Taken Thu 17 Sep", state words (Waiting with the clock, Sending, "Didn't send, no signal"), Retry (failed rows: sets the entry back to queued and flushes) and Remove (56px). "Send now" (`queue-send-now`) is the one hi-vis button, grey and disabled with a timber note while there is no signal or a send is running. The sentence "On an iPhone, photos send only while the app is open, so keep it open until this list is empty." sits under the header. Empty: "Everything's uploaded." (`queue-empty`) with "Upload more photos" to the last job.
- 2026-09-18 Stage 3 (upload/queue): `e2e/flow-b-photos-offline.spec.ts` makes three 8x8 PNGs with a Buffer (own CRC32 + zlib), follows flow b as Alec on Park Rd (stage and category buttons at least 56px, no select inside the screen, offline before upload, the calm note, badge 3, reload, badge still 3 and "3 waiting to send" on the category, queue lists 3 under "64-66 Park Rd, Lock-up: Windows installed", offline off, queue empties and badge goes, uploaded count +3 read from the mock's localStorage records and from part B's gallery category), plus the on-signal path with a removed photo ("All 2 photos uploaded to ..."), Remove on the queue, and Raff on a stage without categories. Every screen asserts no "$".
- 2026-09-18 Stage 3 (upload/queue) screenshot critique: the job name was in the header twice (back link and meta), so the meta now says the stage ("Lock-up stage now"); the queue rows on a 390px phone dropped Remove to its own line, now a third column with Retry stacked above it; the disabled "Send now" still glowed hi-vis offline, now grey with the note saying why.
- 2026-09-18 Stage 3 review fixes (gallery/today): the gallery and Today use part A's shared `useQueuedPhotos()` from `src/components/QueueBadge` (queue and api subscriptions), filtered to the job; the gallery's own hook is gone. Today no longer repeats the shell strip's "N photos waiting to send": the count sits on the Add photos button as a badge (`today-add-badge`, number plus sr-only words), which is what UI_PLAN section 2 asks for. `AlecToday` passes `canComplete={false}` to HoldPointCheck so the tick-off sentence never shows for the site role. The gallery's back link is the shared PageHeader one, so its 56px comes from part A's shell rule.
- 2026-09-18 Stage 3 (upload/queue) review fixes: `public/sw.js` matches the cache with `{ ignoreVary: true }` for the shell fallback, hashed assets and the same-origin fallback (vite preview and some hosts answer `Vary: Origin`, so a `<script crossorigin>` request missed its own precached response and the shell opened offline with no JS). `e2e/offline-shell.spec.ts` loads once, waits for the worker's precache to hold the shell and every script and stylesheet on the page, `context.setOffline(true)`, reloads, and expects the dev bar and nav; it fails against the old worker (checked by hand) and passes now. The worker registers only in production builds, which `vite preview` is.
- 2026-09-18 Stage 3 (upload/queue) review fixes: `flush` re-reads each entry before sending so a Remove tapped during a flush wins; a failure stops the loop only when `canSend()` has turned false, otherwise (a missing job, a bad file) the entry is marked failed with `attempts + 1` and the next photo goes; `src/data/photoQueue.test.ts` covers both, plus single-flight and subscribe. Auto-retry stops after `MAX_AUTO_ATTEMPTS` (5); Retry on the queue screen resets `attempts` to 0 and re-arms it.
- 2026-09-18 Stage 3 (upload/queue) review fixes: queue entries carry `sideId` from queue time and `insertPhoto` files the photo on that side at send time (mockApi `insertPhoto(input, uploadedById, sideId)`), so a side switch between queueing and sending cannot move a photo.
- 2026-09-18 Stage 3 (upload/queue) review fixes: the phone `.page-header__back` 56px rule moved from stepDetail.css to `.shell--phone .page-header__back` in shell.css so every back link on the phone is a tap target; "Different job" on the upload screen is 56px too.
- 2026-09-18 Stage 3 (upload/queue) review fixes: `HoldPointCheck` takes `canComplete` (default: the session role is not `site`); the queued sentence reads "They count once they've sent." for Alec and "You can tick this off once they've sent." for anyone who can tick. StepDetail passes `canTick`; AlecToday needs no change (the default already reads the role).
- 2026-09-18 Stage 3 (upload/queue) review fixes: when signal returns while the held note is up and the batch is still queued, the upload screen shows "Sending N of M" instead of the stale note, then "All N photos uploaded to ..."; a batch that failed for a non-signal reason shows "N photos didn't send" with a link to the queue (`upload-failed`).
- 2026-09-18 Stage 3 (upload/queue) review fixes, FOR PART B: `useQueuedPhotos(jobId?)` in `src/components/QueueBadge.tsx` is the one queue hook (it also subscribes to the shared queue, so sending states show live); PhotoGallery.tsx and AlecToday.tsx should import it from there and drop the copy in PhotoGallery.tsx. CONTRACTS.md has a "Photos and the queue (Stage 3)" section.
- 2026-09-18 Stage 4 (calls): `api.finishCall({ personId, jobs: [{ jobId, itemsUpdated }] })` is the one API addition: it stamps each listed job's `lastConfirmed` with today and logs one `job_confirmed` entry per job as "Dominic rang Raff: 3 items updated on 64-66 Park Rd" ("nothing changed" at zero). No `reviewedAt` field was added to items (types.ts is shared; the job's confirmation is the stamp), and no new activity kind, so notifications and the feed need nothing new. Unit-tested in mockApi.test.ts.
- 2026-09-18 Stage 4 (calls): the cut is UI_PLAN 3.11's (to do or booked, act-by within 14 days or past), which is 11 of Raff's items across 3 jobs in the seed (the Stage 0 note said 9; the defect and the slab inspection are in the cut too). A "Every open item, not just this fortnight's" checkbox (`?scope=all`) widens it, which is what the task's "everything that person owns" reads as. Build jobs are always listed so they can be confirmed; design jobs only when there is something to chase.
- 2026-09-18 Stage 4 (calls): order is "most urgent question first": items still to do before ones already ordered or booked (those only need a confirmation), then act-by, then needed-by; jobs by their first item. That puts Seaview's concrete pump first, then Park Rd's plasterer, then Beatty's tiler, which is flow c's order. The script is planned once per call and held still: marking the pump booked or moving the tiler's date (which pushes Order tiles' act-by out of the fortnight) must not reorder the list or drop a row while Raff is on the phone.
- 2026-09-18 Stage 4 (calls): "Mark confirmed" asks for the date the trade is coming (flow c step 4, "confirmed for the 23rd") and writes it as the item's expected date along with the status; offline it confirms without a date (a tick queues, a date needs signal). Setting an expected date or confirming with one reports the job's finish move from the calculator, "Beatty St finish moves +7 days, $2,000, now Fri 11 Dec" (cost is `slipCost` after minus before through `<Money>`, so it draws nothing for a role without money).
- 2026-09-18 Stage 4 (calls): a handled row folds into the job's "Done this call" strip in a sentence ("booked", "confirmed for Wed 23 Sep", "expected moved to Mon 12 Oct", "skipped, stays for next call") with "Back on the list" to unfold it; a note alone does not fold the row but counts as an update. Finish call lists every job with a "Confirmed today" tick, touched jobs pre-ticked, then a summary per job with the item sentences; "Start another call" clears it. Desktop keys B, C, M, N, S act on the marked row (a raised wash, never orange); the row shows `aria-current`.
- 2026-09-18 Stage 4 (calls) screenshot critique: every phone card had a hi-vis "Mark ..." button (11 orange buttons on one screen), now steel-outlined 56px buttons with hi-vis kept for "Finish call"; the act-by date overflowed its column on both layouts, so the phone puts the date and its words on one line above the item and the desktop column is 9em; "Finish call" moved from the phone header to the foot of the script, where the call ends (desktop keeps it in the header).
- 2026-09-18 Stage 4 (calls), FOR PART A: `e2e/shell.spec.ts` visits `/items/x` and now gets Not found from the item sheet (as `/steps/x` did in Stage 2); that route list needs a seed id such as `/items/it-pr-windows`. Both projects fail on that one test; every other spec passes (140 passed, 8 viewport skips).
- 2026-09-18 Stage 4 (waiting/item): `src/screens/WaitingOn.tsx` (`waiting-on`, `#/waiting`, `?owner=me|<personId>`, `?job=`, `?type=` in the hash query, side automatic) groups open items by a key date: the act-by (rule 1) until the item is confirmed, then the expected or needed-by date, since the acting is done and only the arrival is left. Overdue = the calculator's `isLate` or key date passed; This week and Next week are Mon to Sun from `lastMonday(today)`; Later holds the rest and items with no date; Done is a collapsed `<details>` at the foot (`waiting-group-done`, newest first). Title reads "My items" for `?owner=me` (Raff's tab). Group ids `waiting-group-<key>` exist once per layout (phone sections, desktop `<tbody>`).
- 2026-09-18 Stage 4 (waiting/item): the overdue group is the screen's one loud thing: its count in the display face at hero size, late ink on a 2px late rule, "act-by passed, or late" under it; other groups get a muted count on a concrete rule. No orange on the list, since no single button is the primary action; row buttons are steel-outlined, 56px on the phone.
- 2026-09-18 Stage 4 (waiting/item): phone rows are the shared `<ItemRow>` (link to `#/items/:id`, job name as `context`) plus, in its `action` slot, a strip under the row aligned with the content column: relative words (`waiting-actby-<id>`) and the one button (`item-advance-<id>`). The words never repeat a date the row already shows: to-do rows say "Due tomorrow" / "5 weeks overdue"; booked rows, whose row line says Expected, get "Act by 10 Aug, 5 weeks ago" (no weekday, it shares a 390px line with the button); confirmed rows say "Expected 3 days ago" (amber) or "Expected tomorrow". ItemRow itself is untouched (not this task's file); a `when` override prop on ItemRow would let the list phrase the row's own date line and is reported as a need.
- 2026-09-18 Stage 4 (waiting/item): the advance button says exactly what happens: material "Mark ordered", other to-do types "Mark booked", except decision, reminder and condition of consent which go straight to "Mark done" (the wireframe's `[Done]` on Tile choice); booked "Mark confirmed"; confirmed "Mark done". It calls `updateItemStatus` with that status (not `advanceItemStatus`, which cannot skip booked).
- 2026-09-18 Stage 4 (waiting/item): filters are chips (`waiting-filter-mine|anyone|owner-<id>|all-jobs|job-<id>|all-types|type-<type>`), three rows that scroll sideways on the phone and wrap on the desktop; a job chip also sets the back link and `waiting-add` to `#/items/new?job=<id>`. Owner chips per person are hidden for the builder (Raff's list is his own or anyone's). The desktop table (13px, eleven columns: item, job, type, waiting on, owner, needed by, act by with the age under it, lead, expected with "N days late" under it, status, action) fits a 1280 window beside the sidebar in a `.waiting__scroll` box so the page itself never scrolls sideways; whole rows open the item.
- 2026-09-18 Stage 4 (waiting/item): Alec is refused at the guard (`waiting` and `item` are A P B), so his no-money check on these routes is the shared access message; the API's material-only, build-only scoping was verified in the spec by asserting no design job names and no decision or reminder rows.
- 2026-09-18 Stage 4 (waiting/item): `src/screens/ItemSheet.tsx` (`item-sheet`, `#/items/:id` and `#/items/new?job=&step=&type=`) is one form for reading, adding and editing; every field is a control and Save (`item-save`, the sheet's one hi-vis button, disabled until the draft differs) writes once: `updateItemStatus` when the status changed (so the log reads "X: Confirmed") then `updateItem` for the rest, or `addItem` then a redirect to `#/waiting?job=<id>`. Pickers are buttons: type (`src/components/ItemTypePicker.tsx`, `item-type-<type>`), job (`item-job-<id>`), owner (`item-owner-<personId>`, "(you)" on the current person), status (`item-status-<status>`); steps and trades are native selects (`item-step`, `item-trade`) because thirty steps as buttons would fill the phone; a trade pick fills waiting-on and shows "Ring <trade>, <phone>" as a tel: link (`item-ring`), otherwise free text (`item-waiting-on`).
- 2026-09-18 Stage 4 (waiting/item): the sheet's one figure is the act-by date (`item-act-by`, BigNumber row size, amber when passed): "Act by Fri 18 Sep" with "tomorrow: needed Fri 11 Dec for Plasterboard, minus 12 weeks" under it, recomputed live from the draft (lead time and needed-by), so editing the lead time moves it before Save. Needed-by is read-only on timber paper with "from step X" when a step is linked (the saved item's calculator figure, else the step's forecast start); expected is read-only "Comes from shipment: <name>" with a link when a shipment is linked. New items default the owner to the current person (the draft re-seeds on `personId` because the provider applies `?as=` after the first render).
- 2026-09-18 Stage 4 (waiting/item): validation is a words list (`item-problems`): "Give it a title.", "Pick a job.", "Pick a step, or set a needed-by date." (design jobs: "Set a needed-by date."), "Lead time is whole weeks, 0 or more.", "The confirmed date is after today.". Offline, the date inputs are disabled with "Needs signal"; status buttons and Save stay live (status ticks queue). Delete is admin only (`item-delete`, inline "Delete it / Keep it", `item-delete-confirm`), per the task; UI_PLAN's "and partners" can be widened later. History (`item-history`) lists `listActivity({ itemId })`.
- 2026-09-18 Stage 4 (waiting/item): `e2e/shell.spec.ts`'s route list now visits `/items/it-pr-windows` instead of `/items/x`, since a missing item is Not found (as Stage 2 did for steps). `e2e/waiting-item.spec.ts` covers groups, the overdue words, advancing (windows: confirmed lands in Later by its expected date, then done), the decision's "Mark done", the job filter, the lead-time edit moving act-by, a new reminder on Park Rd landing in Next week, delete rights, offline, Alec on every route, and the desktop columns with no sideways page scroll.
- 2026-09-18 Stage 4 (waiting/item) screenshot critique: the phone strip's words hung in the left gutter under the type word, breaking the row's two-column rhythm; the strip is now indented to the content column with 13px words and a tighter button. Desktop: 11 columns first overflowed 1280 by 300px; nowrap was limited to dates, the age and the late text dropped to a second line in their cells, the Item column's underline stays (base link style).
- 2026-09-18 Stage 4 review fixes (calls): CallItem imports `nextStatus` from part A's `src/domain/itemFlow.ts` and `agoWords` from `src/domain/dates.ts`; the local copies are gone, and the folded "Done this call" sentence uses the button's own words ("Mark requested" folds to "requested"), so a consultant report reads the same on the call list, the waiting-on list and the item sheet. The flow-c spec asserts it as Dom ringing Dominic (glazing certificate "Mark requested", stormwater council request "Mark received").
- 2026-09-18 Stage 4 review fixes (calls): the desktop date inputs get a 9em column (the row's fourth column is 19em) so a value is never clipped; the finish panel words an unticked job "Not confirmed" and a ticked one "Confirmed today", each with its item count.
- 2026-09-18 Stage 4 (waiting/item) review fixes: `src/domain/itemFlow.ts` is the one status flow (`nextStatus`, `advanceLabel`), unit-tested in `src/domain/itemFlow.test.ts`; locked words: report and council request go Mark requested → Mark received → Mark done, decisions, reminders and conditions straight to done, trade/inspection/defect Mark booked, material Mark ordered. `agoWords` moved to `src/domain/dates.ts` (tested there too); WaitingOn's copies are gone and the call list imports the same functions.
- 2026-09-18 Stage 4 (waiting/item) review fixes: `ItemRow` gained a backwards-compatible `when` override, so the waiting-on row's own date line now reads "Act by Mon 10 Aug, 5 weeks ago" (amber once passed) or, once confirmed, "Expected Wed 16 Sep, yesterday"; late items keep the calculator's phrase. The phone strip under the row holds only controls, right-aligned: Call (tel: link when the trade has a number), Set date, and the status button; the desktop action cell stacks Call and Set date as small links under the button so the table still fits 1280.
- 2026-09-18 Stage 4 (waiting/item) review fixes: "Set date" is inline on the row (wireframe), a date input with Save date / Cancel through `api.setItemExpectedDate`; hidden with "Date needs signal" offline; not offered on rows whose expected date comes from a shipment. Status, waiting-on and time filters from UI_PLAN 3.8 are covered by the groups plus the Done details rather than extra chips.
- 2026-09-18 Stage 4 (waiting/item) review fixes: the desktop expected cell now shows the amber "Wed 16 Sep / yesterday" for a confirmed item past its date, matching the phone; the sheet's late words come from `forecast.items[id].isLate` alone ("Expected Mon 5 Oct, 7 days late" on Beatty's tiler), not only shipment-linked items.
- 2026-09-18 Stage 4 (waiting/item) review fixes: delete is admin and partners (UI_PLAN 3.10). The add form adapts minimally: a defect gets an optional photo, either one of the job's six newest photos as thumbnail buttons (`item-photo-pick-<id>`) or a "Take a photo" link to `#/jobs/<id>/upload?item=<itemId>` (the upload screen does not read `?item=` yet; reported as a need for part A of Stage 3's owner or Stage 5). Consultant report and council request keep the lead time: UI_PLAN 3.10 does not say to hide it.
- 2026-09-18 Stage 4 (waiting/item) review fixes: `mockApi.addItem` throws when `stepId` is not on `jobId` (unit test in mockApi.test.ts). CONTRACTS.md has "Waiting-on list and item sheet (Stage 4)" with the query contract, ids, exports and the item flow.

- 2026-09-18 Stage 5 (design/notifications): `src/screens/DesignChecklist.tsx` (`checklist`, the `/jobs/:id` route for design jobs) draws the stages as a ladder of drawn tick boxes (inline SVG: empty, half-filled for under way, filled with a tick for done), the current stage's name in the display face, and its open items indented under it with "outstanding 23 days" in words (amber wash past 14 days, the jobs list's threshold). The hero is the calculator's `checklist.outstanding` and `oldestDays` in the jobs list's exact words ("2 outstanding, oldest 23 days", `checklist-outstanding`), so the two screens cannot disagree. Meta reads "DA, through council", "Now: with council" (`checklist-current`) and the freshness words. Tabs: Checklist and Waiting on (`#/waiting?job=<id>`); "Add item" links to `#/items/new?job=<id>`.
- 2026-09-18 Stage 5 (design/notifications): items on design jobs carry no stage, so every open item sits under the current stage (UI_PLAN 3.7 "under the current stage") and done ones fold into a `<details>` at the foot (`checklist-done`); if every stage is done, open items list under "Still open". Empty: "Nothing outstanding at this stage." (`checklist-empty`).
- 2026-09-18 Stage 5 (design/notifications): stage status is one segmented control per stage (three joined buttons `checklist-stage-status-<id>-<not_started|in_progress|done>`, `aria-pressed`, the pressed one filled steel) for admin and builder; partners see the words ("Not started", "Under way", "Done", `checklist-stage-words-<id>`). It calls the existing `api.updateStage(id, { status })`, whose log line is "Updated stage With council"; better words would need a shared-file edit (reported). Status ticks stay live offline (they queue).
- 2026-09-18 Stage 5 (design/notifications): `fireRemindersDueToday` (mockApi, one function edited) now also raises: "act by Mon 10 Aug has passed" (kind `overdue`) for to-do items whose act-by passed but are not yet late; `job_unconfirmed` for amber build jobs to every admin and partner on the side, deduplicated per job and day (`already` now narrows on `jobId` when given); hold-point-a-week-away text carries the readiness words from `holdPointReadinessWords` ("is on Mon 28 Sep: 1 of 3 required photo sets uploaded. Still empty: ..."); and a new kind `photos_uploaded` (one line added to `NotificationKind` in types.ts) to builders and admins when a required category of an open hold point got photos today ("Alec added 4 photos for Slab inspection before pour at 31 Seaview St: 1 of 3 required photo sets uploaded"). ETA changes still raise from `setShipmentEta`. On 17 Sep Raff gets two (slab steel, tiler), Dominic one plus Beatty unconfirmed; the partners get Beatty unconfirmed.
- 2026-09-18 Stage 5 (design/notifications): `src/screens/Notifications.tsx` (`notifications`, `#/notifications?tab=activity&job=<id>`) is two tabs on the phone (`notifications-tab-mine`, `notifications-tab-activity`) and two columns on the desktop, because the sidebar's "Activity" link opens this same route and Dominic reads both at once. Rows are grouped by day ("Today", "Yesterday", "Tue 15 Sep") with the time in a tabular column. Notification rows (`notification-<id>`, `data-read`) are links to the thing (item, step, shipment, else job; Alec's item links fall back to the job because he has no item screen) that mark the notification read on tap; unread rows are bold with the word "New" in steel; `notifications-mark-all` is disabled at zero; empty is "Nothing new." Activity rows (`activity-<id>`) read time, who (bold), words, with the job name on its own line under the words only when the words do not already name it; filters are job buttons (`activity-filter-all`, `activity-filter-<jobId>`, `aria-pressed`). Alec gets notifications only (`canSee('activity')` false: no tab, no column).
- 2026-09-18 Stage 5 (design/notifications): `src/components/Buzz.tsx` (`buzz`, `buzz-link`, `buzz-dismiss`, `buzz-more`) is mounted once by the shell with `layout`. It watches the current person's unread notifications and keeps a seen set per person and side, initialised to what is already there, so seeded notifications and a person switch never buzz; a new id shows the newest (among same-minute stamps, the last raised) with "and N more" linking to the list. Dark steel banner, one line, hi-vis "Open item" link (marks it read), "Dismiss"; hides after 8s; `role="status" aria-live="polite"`; the slide-up is dropped under reduced motion. Phone: fixed above the tab bar; desktop: at the foot of the content column beside the sidebar. Never a modal.
- 2026-09-18 Stage 5 (design/notifications): `src/components/BellBadge.tsx` (`bell-badge`) replaces the shell's local `UnreadCount` (`unread-count` is gone; nothing used it) in both the top bar and the sidebar foot; AppShell's only other edit is the `<Buzz>` mount after the content. `nav.ts` still sends the sidebar "Activity" link to `/notifications` (not owned here; the desktop shows both columns, so it lands on the feed anyway).
- 2026-09-18 Stage 5 (design/notifications) screenshot critique: the phone checklist had twelve loose status buttons (three per stage, 56px each), the same fault Stage 4's call list had; they are now one joined segmented control per stage at 48px (36px on the desktop). Also fixed: the activity job link ran on from the sentence ("Roof plumbing started 64-66 Park Rd"), now its own muted line; "and 1 more" wrapped mid-phrase in the desktop buzz.
- 2026-09-18 Stage 5 (design/notifications): `e2e/design-notifications.spec.ts` (18 tests, both projects) covers West St's words against the jobs list, the other three design jobs, stage status through the buttons persisting across a reload and moving the items to the next stage, the partner's read-only view, Raff firing reminders (buzz words, "and 1 more", bell count 2, mark all read hides the badge, a second fire raises nothing), the buzz link opening the item sheet, the seeded feed and the Beatty filter, a stage change landing in the feed, and Alec's list (his own only, no feed, no "$"). Full suite on port 4202: 194 passed, 10 viewport skips.- 2026-09-18 Stage 5 (holdpoint/notes): `HoldPointCheck` now takes `queuedByCategory` and `addHref(categoryId)`: each empty required set gets its own "Add photos" link (`holdpoint-add-<categoryId>`, `#/jobs/<id>/upload?stage=&category=&return=/steps/<stepId>`) and its own "2 waiting to send, they don't count yet" line (`holdpoint-queued-<categoryId>`); filled sets get no link, since the block's job is getting the empty ones filled. The refusal (`holdpoint-refusal`, text unchanged) is drawn first in the block, directly under Mark done. When every set has a photo a green sentence says so (`holdpoint-ready`: "Every required set has a photo. You can tick this off."). The generic "Add photos now" (`holdpoint-add-photos`) keeps its Stage 2 href, which two earlier specs assert. Rows are one line on the desktop shell, two on the phone (link at 56px).
- 2026-09-18 Stage 5 (holdpoint/notes): the readiness words moved to the calculator as `holdPointReadinessWords(check)` (`src/domain/forecast.ts`, beside `holdPointRefusalText`); `readinessWords` in HoldPointCheck.tsx re-exports it. New `api.holdPointReadiness(stepId)` returns `{ stepId, stepName, jobId, jobName, stageId, forecastStart, filled, total, words, missingCategories, ok, check }` (undefined for non hold points), for part B's "hold point in a week, 1 of 3 sets uploaded" notifications.
- 2026-09-18 Stage 5 (holdpoint/notes): StepDetail reads the queue through the shared `useQueuedPhotos(jobId)` (so the counts move as a flush runs) instead of its own effect, and drops a stale hold-point refusal by itself once the check turns ok.
- 2026-09-18 Stage 5 (holdpoint/notes): PhotoUpload reads `?return=<path>` (Done and the back link go there: "Back to the step" / "Back to the notes") and `?item=<itemId>` (after a send with signal, the first photo that landed is attached to that item via `updateItem({ photoId })` when the item has none), the Stage 4 need. `?category=` was already read.
- 2026-09-18 Stage 5 (holdpoint/notes): `DailyNote` gained optional `weather` ('fine' | 'overcast' | 'rain' | 'wind' | 'hot'), `onSite` (person and trade ids), `photoIds` and `queued`; `DailyNoteInput` takes them; `updateDailyNote(id, string | DailyNotePatch)` keeps the Stage 0 string form; new `flushDailyNotes()` clears `queued` on every note while there is signal. The mock stores locally either way, so "queued" is only the flag and the words; the notes screen calls `flushDailyNotes` on mount and whenever `offline` turns false. Four seeded Park Rd notes carry weather and on-site so the list reads like a diary.
- 2026-09-18 Stage 5 (holdpoint/notes): `src/screens/DailyNotes.tsx` (`#/jobs/:id/notes`, `notes`): phone is today's page first (`note-today`: `src/components/NoteEntry.tsx`, the day in words as a 28px display heading, a timber-paper `note-text` box, weather as five 56px radio buttons `note-weather-<key>`, "On site" as 56px toggle chips `note-onsite-<id>` for the side's site/builder/admin people then the trades whose type matches a step running within two weeks of today (trade items are hidden from Alec, so steps and requirements are the source), the rest behind "More trades (13)" (`note-more-trades`), today's own photos as thumbnails with "Add a photo" (`note-add-photo`, upload with `?return=`), one 64px hi-vis `note-save`), then earlier days grouped by week (`notes-week-<monday>`: "This week, 14-18 Sep", "Last week, 7-11 Sep", "Week of ..."; rows `note-<id>` with "Overcast. On site: Alec, Cladder." under the date). Desktop is the same days as a table (Day, Note, Weather, On site, By) with the entry in a sticky 360px side panel. Alec, Raff and Dominic write; partners get no panel. Today's note by the current person is edited in place ("Save changes"); an empty save is refused in words (`note-problem`). After a save the words under the button come from the saved record: `note-saved` "Saved. You can change it until the end of the day." or `note-queued` with the clock "Saved on this phone, waiting to send. It goes when you're back in range."; queued rows in the list carry the clock and "waiting to send". Empty: "No notes yet. One line a day is plenty." (`notes-empty`).
- 2026-09-18 Stage 5 (holdpoint/notes): `e2e/flow-e-hold-point.spec.ts` runs UI_PLAN flow e as Raff with today 2026-09-28 (reset, then reload so the URL's date applies again): refusal naming both empty sets, the row links with the exact href, upload of one generated PNG per set with Done returning to the step, refusal naming the last set, `holdpoint-ready`, Mark done to Done, the Seaview overview reading "No hold points left", reset; plus a queued case (two photos saved without signal read "2 waiting to send, they don't count yet" on the row, Mark done disabled with "Needs signal", signal back moves the count to 2 photos). `e2e/daily-notes.spec.ts`: Alec on Park Rd on 18 Sep (56px buttons on the phone project, no select, empty save refused, Overcast and Alec and Cladder saved), offline note on Beatty queued with the clock, signal back clears it, Dominic reads both (table and panel on the desktop), Dom reads without a panel, no "$" as Alec, reset. Both projects.
- 2026-09-18 Stage 5 (holdpoint/notes) screenshot fixes: `.display` sets its own font-size, so the diary dates needed a more specific selector (the desktop table's Day column had blown up to 56px and pushed the panel over the table); the weather row is a wrapping flex row (a five-column grid squeezed "Overcast" out of its chip in the 360px panel); hold-point rows on the desktop are one line (name, queued words, count, link) instead of the phone's two.

## Open problems

(none: the four Stage 0 questions were decided by the orchestrator on 2026-09-18, see Decisions A to D)

## Reviewer sign-offs

- Stage 0: signed off by reviewer, 2026-09-18, tests 49/49, e2e 6/6
- Stage 1: signed off by reviewer, 2026-09-18, tests 49/49, e2e 86/86
- Stage 2: signed off by reviewer, 2026-09-18, tests 50/50, e2e 111/111 (7 viewport-gated skips, each run in the other project)
- Stage 3: signed off by reviewer, 2026-09-18, tests 53/53, e2e 133/133 (7 viewport-gated skips, each run in the other project)
- Stage 4: signed off by reviewer, 2026-09-18, tests 58/58, e2e 168/168 (10 viewport-gated skips: program 3+3 and waiting-item 1+1 run in the other project; flow-c keys and flow-d add form are desktop-only)
